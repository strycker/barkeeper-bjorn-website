---
phase: 07-ai-integration
plan: 05
type: execute
wave: 4
depends_on: [01, 03, 04]
files_modified:
  - app/js/views/recommender.js
  - app/js/views/inventory.js
  - app/js/views/dashboard.js
  - app/js/views/bartender-wizard.js
  - app/js/views/recipes.js
  - app/js/recommender-engine.js
  - app/css/app.css
autonomous: false
requirements: [REC-04, AI-04, AI-05, AI-12, AI-03, AI-13]
must_haves:
  truths:
    - "Every recommender recipe card and every Recipes-tab chip has an 'Ask Bjorn about this' action that opens the shared quick-ask drawer seeded with the recipe + the user's profile/mood (REC-04/AI-04)"
    - "Inventory and Dashboard expose an AI-05 'best single bottle to add' entry that opens the drawer seeded with inventory + vetoes context; the Dashboard 'Chat with Bjorn' / 'Classroom' coming-soon cards are un-disabled into real #chat / #classroom links"
    - "The Bartender Wizard has a 'Help me write this with Claude' button that drafts long-form persona text from the short preference inputs and fills the textarea for the user to edit before the existing save (AI-12)"
    - "Generate with AI on a new recipe produces a schema-valid draft via ClaudeAPI.requestJSON, auto-saves it to drafts (D-09), and keeps a refine card open for in-place tweaks (same draft) and 'generate new' (new draft) (AI-03/D-10)"
    - "The Recipes view has a Drafts tab listing draft chips; each chip has 'Promote to Original' which re-tags _source to 'originals', assigns a cocktail<ts> id, moves it into recipes.originals, removes it from drafts, and saves sequentially (recipes then drafts) (D-11)"
    - "All AI writes to drafts/recipes go through WriteGate (schema-validate -> diff -> confirm); a phantom-ingredient flag from WriteGate.inventoryFidelity is surfaced in the draft preview (FM #3)"
    - "AI-13: with a key set, recommender-engine.js calls a key-gated, fail-soft, cached Claude fallback (RecommenderEngine.deriveWithAI -> ClaudeAPI.callMessages + extractJSON) after the static DERIVATIONS miss; with no key the engine behaves byte-identically to Phase 5 (no network call, no behavior change)"
  artifacts:
    - path: "app/js/views/recipes.js"
      provides: "AI-03 generate->draft->refine, Drafts tab, Promote-to-Original, REC-04 on chips"
      contains: "requestJSON"
    - path: "app/js/views/recommender.js"
      provides: "REC-04/AI-04 Ask-Bjorn button on cards seeding the drawer"
      contains: "openDrawer"
    - path: "app/js/views/inventory.js"
      provides: "AI-05 best-bottle entry seeding the drawer"
      contains: "openDrawer"
    - path: "app/js/views/dashboard.js"
      provides: "AI-05 entry + un-disabled #chat/#classroom cards"
      contains: "href=\"#chat\""
    - path: "app/js/views/bartender-wizard.js"
      provides: "AI-12 Help-me-write-this-with-Claude persona drafting"
      contains: "requestJSON"
    - path: "app/js/recommender-engine.js"
      provides: "AI-13 deriveWithAI fallback after static DERIVATIONS, cached in bb_derivation_cache, key-gated, fail-soft"
      contains: "deriveWithAI"
  key_links:
    - from: "app/js/views/recommender.js"
      to: "ChatView.openDrawer"
      via: "Ask Bjorn button -> openDrawer({seed: recipe+profile+mood})"
      pattern: "openDrawer"
    - from: "app/js/views/recipes.js"
      to: "ClaudeAPI.requestJSON"
      via: "Generate with AI -> requestJSON(schemaKey:'drafts')"
      pattern: "requestJSON"
    - from: "app/js/views/recipes.js"
      to: "WriteGate.gate"
      via: "draft auto-save + promote through schema->diff->confirm"
      pattern: "WriteGate"
    - from: "app/js/views/recipes.js"
      to: "recipes.originals"
      via: "Promote-to-Original moves entry + sequential State.save"
      pattern: "originals"
    - from: "app/js/recommender-engine.js"
      to: "ClaudeAPI.callMessages"
      via: "deriveWithAI(ingredient, tokens) -> callMessages + extractJSON on cache miss"
      pattern: "deriveWithAI"
---

<objective>
Wire AI into the five existing views: add the REC-04/AI-04 "Ask Bjorn about this" entry to every recommender card and Recipes chip (seeds the shared drawer with the recipe + profile/mood); add the AI-05 best-bottle entry to Inventory + Dashboard (and un-disable the Dashboard coming-soon `#chat`/`#classroom` cards); add the AI-12 "Help me write this with Claude" persona drafter to the Bartender Wizard; and upgrade the Recipes "Generate with AI" flow into a real AI-03 design → auto-saved draft (D-09) → refine card (D-10) → Drafts tab + Promote-to-Original (D-11), all written through the 07-01 `WriteGate` (schema-validate → diff → confirm) with phantom-ingredient flagging.

Purpose: This converts the chokepoint + drawer + write-gate primitives into the user-visible AI features that make Bjorn feel present everywhere (D-01). The entry points are light (drawer seeds); AI-03 is the heavyweight that must never silently corrupt recipe data — hence WriteGate on every write.
Output: Extended `recommender.js`, `inventory.js`, `dashboard.js`, `bartender-wizard.js`, `recipes.js`; AI CSS.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-integration/07-CONTEXT.md
@.planning/phases/07-ai-integration/07-AI-SPEC.md
@.planning/phases/07-ai-integration/07-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Extracted from the codebase. Use directly — do not re-explore. -->

From 07-01 (claude-api.js + write-gate.js):
```javascript
ClaudeAPI.requestJSON({ system, userPrompt, schemaKey, model, maxTokens });  // parse->Normalize->validate->ONE retry->throw (fail-closed). Returns normalized object.
WriteGate.validate(schemaKey, payload);            // -> [] valid, else error strings
await WriteGate.gate({ schemaKey, oldData, newPayload, message, onConfirm });  // schema->diff preview->confirm->onConfirm(); blocks if invalid
WriteGate.inventoryFidelity(recipe, inventoryTokens, vetoes);  // -> { phantoms:[...], vetoed:[...] }
```
From 07-03: `ChatView.openDrawer({ seed })` — shared ephemeral drawer, self-gates on key.

drafts State file (from 07-01): `State.get('drafts')` -> `{ drafts:[...], last_updated }`; each draft is a universal recipe + `_source:'ai-generated'`, `draft_id`, `created_at`, `updated_at`, `source_prompt`. `State.patch('drafts', fn)` + `State.save('drafts')`.

recommender.js card actions (recommender.js:85-89 `.rec-card-actions`; cards built in `_renderCard` :59, `_renderTwoAwayCard` :116; wired in `_rerender` :219-499). Mood/profile floats via `_getInitialSliderValues`/`_normalizeProfile`.

recipes.js: tabs at :38-44 (originals/favorites/wishlist/made — ADD "drafts"); `renderTab` :157; chip render `renderRecipeChips` :231-287; generate entry `showAIPromptModal` :77,:107 (today only copies a prompt — REPLACE with real design); `buildPromptContext()` :81-105 (reuse as the seed); originals add/id convention `id:'cocktail'+Date.now()` matches `^cocktail[0-9]+$` (recipes.schema.json:59).

inventory.js: `return { render }` at :884; section grids render bottles — add an AI-05 button near the top toolbar.

dashboard.js: disabled cards at :128-152 (`.menu-item--disabled [data-coming-soon]` + toast gate) — UN-DISABLE "Chat with Bjorn" -> `<a href="#chat">`, "Classroom" -> `<a href="#classroom">`; ADD a Library card and an AI-05 inventory-advice entry. Live card markup to copy: dashboard.js:117-121.

bartender-wizard.js: personality textarea `#bw-personality` (:240) + input handler `State.patch('barkeeper',...)` (:300-302). ADD the AI-12 button next to the textarea.

Utils: `Utils.escapeHtml` (utils.js:26-32) MANDATORY on all model output before innerHTML; `Utils.showToast`; `Utils.sameRecipe(a,b)` (utils.js:113-117) for promote dedup.

Inventory tokens + vetoes for AI-05 seed + inventoryFidelity: read from `State.get('inventory')` (bottle names + `vetoes`), normalized lowercase (reuse the recommender engine's token approach).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drawer-seeding entry points — REC-04/AI-04 (cards+chips), AI-05 (Inventory+Dashboard), AI-12 (Wizard), Dashboard un-disable</name>
  <read_first>
    - app/js/views/recommender.js:59-160 (_renderCard/_renderTwoAwayCard + .rec-card-actions) + :219-499 (_rerender wiring)
    - app/js/views/recipes.js:231-287 (renderRecipeChips — add Ask-Bjorn to chips)
    - app/js/views/inventory.js (top toolbar region) + dashboard.js:99-152 (menu grid + disabled cards)
    - app/js/views/bartender-wizard.js:236-246,300-302 (personality textarea + handler)
    - app/js/views/chat.js (ChatView.openDrawer) + 07-CONTEXT.md D-01 (all entry points open the shared drawer)
  </read_first>
  <behavior>
    - Each recommender card (buildable, one-away, two-away) and each Recipes chip gains an "Ask Bjorn about this" button. With a key: opens `ChatView.openDrawer({ seed })` seeded with the recipe name/ingredients + the user's flavor profile + current mood (REC-04/AI-04). With no key: the openDrawer self-gate shows the no-key affordance.
    - Inventory (toolbar) and Dashboard each gain an AI-05 "Best bottle to add" entry that opens the drawer seeded with the inventory + vetoes + gaps context ("what single bottle should I add next, and why?").
    - Dashboard's two disabled coming-soon cards become real `<a href="#chat">` and `<a href="#classroom">` links; a Library card is added; the existing key-gate toast remains as the CHAT-03 affordance only where appropriate.
    - The Bartender Wizard gains a "Help me write this with Claude" button beside #bw-personality that drafts persona text from the short inputs (name/voice/preferences) and fills the textarea (user edits, then the existing input handler saves via State.patch).
  </behavior>
  <action>
    1. recommender.js: in `_renderCard` and `_renderTwoAwayCard`, add an "Ask Bjorn" `<button data-ask-recipe="<idx>">` into `.rec-card-actions`; in `_rerender`, delegate clicks to call `ChatView.openDrawer({ seed: 'Tell me about the '+name+'. Would it suit my taste, and what variations would you suggest given my bar?' })` built from the card's recipe + `_normalizeProfile`/mood floats. Escape any value placed in the DOM (the seed string goes to the model).
    2. recipes.js: in `renderRecipeChips` (:231-287) add the same "Ask Bjorn" button per chip wired to `ChatView.openDrawer`.
    3. inventory.js: add a toolbar button "Best bottle to add (AI)" → `ChatView.openDrawer({ seed: 'Given my current inventory and vetoes, what single bottle should I add next, and why?' })`.
    4. dashboard.js: replace the two `.menu-item--disabled [data-coming-soon]` cards (:128-143) with live `<a href="#chat" class="menu-item">Chat with Bjorn</a>` and `<a href="#classroom" class="menu-item">Classroom</a>` (copy the live-card markup at :117-121); add a `<a href="#library" class="menu-item">Library</a>` card and a "Best bottle to add (AI)" action that calls `ChatView.openDrawer` with the AI-05 seed. Keep the toast handler only for any still-gated affordance.
    5. bartender-wizard.js: add a button `#bw-ai-help` next to #bw-personality; on click (key-gated) call `await ClaudeAPI.requestJSON({ system:'You draft a bartender persona description.', userPrompt:<short inputs>, schemaKey:'barkeeper', model: ClaudeAPI.getModel(), maxTokens: 512 })` OR a free-text `callMessages` if a JSON shape is overkill — fill `#bw-personality.value` with the result (escaped only when shown elsewhere; textarea value assignment is safe) and dispatch its `input` event so the existing State.patch handler saves. No write to GitHub without the user editing+the existing save path.
    6. app/css/app.css: append `.ai-ask-btn`, `.ai-advice-btn` styles.
    All entry points reuse the shared drawer (D-01) — no bespoke inline chat panels.
  </action>
  <verify>
    <automated>node --check app/js/views/recommender.js && node --check app/js/views/recipes.js && node --check app/js/views/inventory.js && node --check app/js/views/dashboard.js && node --check app/js/views/bartender-wizard.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` exits 0 for all five view files.
    - `grep -c "openDrawer" app/js/views/recommender.js` ≥1, `app/js/views/recipes.js` ≥1, `app/js/views/inventory.js` ≥1, `app/js/views/dashboard.js` ≥1.
    - `grep -c 'href="#chat"' app/js/views/dashboard.js` ≥1 and `grep -c 'href="#classroom"' app/js/views/dashboard.js` ≥1 (cards un-disabled); `grep -c "data-coming-soon" app/js/views/dashboard.js` is reduced from baseline (at least the chat/classroom ones removed).
    - `grep -c "bw-ai-help\|requestJSON\|callMessages" app/js/views/bartender-wizard.js` ≥1 (AI-12 drafter present).
    - No bespoke streaming panel added: `grep -c "streamMessage" app/js/views/recommender.js app/js/views/inventory.js app/js/views/dashboard.js` returns 0 across these (they reuse the drawer).
  </acceptance_criteria>
  <done>Every card/chip seeds the drawer for REC-04/AI-04; Inventory+Dashboard expose AI-05; Dashboard cards are live #chat/#classroom/#library links; the Wizard drafts persona text via Claude (AI-12).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AI-03 — Generate with AI -> schema-valid draft (requestJSON) -> auto-save (WriteGate) -> refine card</name>
  <read_first>
    - app/js/views/recipes.js:76-155 (showAIPromptModal/handleGenerate — the flow to replace) + :81-105 buildPromptContext (reuse as seed)
    - app/js/claude-api.js requestJSON + app/js/write-gate.js gate/validate/inventoryFidelity (07-01)
    - schema/drafts.schema.json (draft item shape from 07-01) + app/js/state.js drafts save path
    - .planning/phases/07-ai-integration/07-CONTEXT.md D-09/D-10/D-11 + 07-AI-SPEC.md §4b (requestJSON contract) + §6 (write-safety guardrails)
  </read_first>
  <behavior>
    - Clicking "Generate with AI" with a prompt calls `ClaudeAPI.requestJSON({ schemaKey:'drafts', ... })` seeded by buildPromptContext(); a valid draft is produced; an invalid/unparseable response fails closed (error shown, NO write).
    - The valid draft is auto-saved to drafts immediately (D-09: an interrupted connection must not lose it) via the WriteGate path (schema-validate -> diff -> confirm) — for the auto-save the gate may render a brief confirm or auto-confirm the first save per D-09, but a schema-INVALID payload is NEVER written.
    - The generated draft preview surfaces a phantom-ingredient flag from WriteGate.inventoryFidelity (ingredients not in inventory and not flagged as substitutions) so the user sees buildability before promoting (FM #3).
    - A refine card stays open (D-09): an in-place tweak ("make it less sweet", "different name") updates the SAME draft (D-10); "generate a new recipe from this" creates a NEW draft; a "Save draft copy before refining" button forks the open draft as a separate save.
  </behavior>
  <action>
    1. recipes.js: replace `showAIPromptModal`/the copy-only path with `async function handleGenerate(promptText)`: build `userPrompt` from `buildPromptContext()` + the user's prompt; `const draft = await ClaudeAPI.requestJSON({ system:<recipe-design persona + JSON-only + universal recipe schema instruction>, userPrompt, schemaKey:'drafts', model:ClaudeAPI.getModel(), maxTokens:1024 })`. Wrap in try/catch → on throw show the error (escaped) and DO NOT write (fail-closed).
    2. Auto-save (D-09): compute `const tokens = <lowercased inventory bottle names>`, `const fid = WriteGate.inventoryFidelity(draft, tokens, vetoes)`; call `WriteGate.gate({ schemaKey:'drafts', oldData: State.get('drafts'), newPayload: <drafts with the new entry appended, _source:'ai-generated', draft_id, created_at, source_prompt>, message:'Save AI draft', onConfirm: () => { State.set('drafts', newDraftsObj); return State.save('drafts'); } })`. Surface `fid.phantoms`/`fid.vetoed` (escaped) in the gate's diff preview so phantom bottles are visible before promote (FM #3).
    3. Refine card (D-10): render the open draft with a refine input + buttons: "Apply tweak" (re-runs requestJSON with the existing draft + the tweak, updates the SAME draft_id via State.patch('drafts',...) through WriteGate), "Generate new from this" (creates a NEW draft_id), and "Save draft copy before refining" (clones the current draft as a new entry then saves). Each write goes through WriteGate. Escape all model text before innerHTML.
    Use the universal recipe JSON shape so drafts render as chips everywhere (D-11). Never write a payload that failed WriteGate.validate.
  </action>
  <verify>
    <automated>node --check app/js/views/recipes.js && grep -c "requestJSON\|WriteGate" app/js/views/recipes.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/js/views/recipes.js` exits 0.
    - `grep -c "requestJSON" app/js/views/recipes.js` ≥1 (structured generation) with `schemaKey` 'drafts' present (`grep -c "drafts" app/js/views/recipes.js` ≥1).
    - `grep -c "WriteGate" app/js/views/recipes.js` ≥1 and every drafts write is inside a WriteGate.gate call (manually confirm no `State.save('drafts')` outside an onConfirm).
    - `grep -c "inventoryFidelity" app/js/views/recipes.js` ≥1 (phantom flag surfaced).
    - `grep -c "draft_id" app/js/views/recipes.js` ≥1 (same-vs-new draft tracking for D-10).
    - `grep -c "escapeHtml" app/js/views/recipes.js` increased vs. baseline (model output escaped in the refine card/preview).
  </acceptance_criteria>
  <done>Generate produces a schema-valid draft via requestJSON, auto-saves it through WriteGate with a phantom-ingredient flag, and keeps a refine card open for same-draft tweaks / new-draft generation / fork-before-refine — all fail-closed.</done>
</task>

<task type="auto">
  <name>Task 3: Drafts tab + Promote-to-Original (D-11)</name>
  <read_first>
    - app/js/views/recipes.js:38-63 (tab bar) + :157 renderTab + :231-287 renderRecipeChips (chip render to reuse for drafts)
    - app/js/views/recipes.js originals add path + id convention `cocktail<ts>` (recipes.schema.json:59 ^cocktail[0-9]+$)
    - app/js/state.js drafts + recipes save (sequential save, Pitfall 4)
    - app/js/write-gate.js (gate for the promote write) + utils.js:113-117 Utils.sameRecipe (dedup)
    - .planning/phases/07-ai-integration/07-CONTEXT.md D-11 (Drafts tab + Promote re-tag)
  </read_first>
  <behavior>
    - A "Drafts" tab appears in the Recipes view, listing `State.get('drafts').drafts` as chips (reusing the existing chip render since drafts conform to the universal recipe JSON).
    - Each draft chip has a "Promote to Original" action: it re-tags `_source` from 'ai-generated' to 'originals', assigns `id: 'cocktail'+Date.now()` (matches ^cocktail[0-9]+$), appends to `recipes.originals`, removes the entry from `drafts`, then saves recipes THEN drafts sequentially (Pitfall 4 — avoid 409).
    - Promotion goes through WriteGate (schema-validate -> diff -> confirm) for the recipes write; a duplicate (Utils.sameRecipe against existing originals) is flagged in the diff preview before confirm.
  </behavior>
  <action>
    1. recipes.js: add a `<div class="tab" data-tab="drafts">Drafts (${draftCount})</div>` to the tab bar (:38-44) and a `renderTab('drafts', ...)` branch that renders draft chips via `renderRecipeChips` (drafts are universal recipe JSON). Each draft chip gets a "Promote to Original" button `data-promote="<draft_id>"`.
    2. Wire promote: build `promoted = { ...draft, _source:'originals', id:'cocktail'+Date.now() }` (drop draft-only keys draft_id/source_prompt as appropriate). Run through `WriteGate.gate({ schemaKey:'recipes', oldData: State.get('recipes'), newPayload:<recipes with promoted appended>, message:'Promote to Original', onConfirm: async () => { State.set('recipes', recipesWithPromoted); await State.save('recipes'); State.patch('drafts', d => { d.drafts = d.drafts.filter(x => x.draft_id !== draft.draft_id); }); await State.save('drafts'); } })`. In the diff preview, if `Utils.sameRecipe(promoted, existingOriginal)` matches, surface a "possible duplicate" note (escaped). Saves are sequential (recipes then drafts).
    3. Re-render the Drafts + Originals counts after a successful promote; toast confirmation.
    Escape all rendered draft fields before innerHTML.
  </action>
  <verify>
    <automated>node --check app/js/views/recipes.js && grep -c "data-tab=\"drafts\"\|Promote" app/js/views/recipes.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/js/views/recipes.js` exits 0.
    - `grep -c 'data-tab="drafts"' app/js/views/recipes.js` returns 1 (Drafts tab).
    - `grep -c "Promote" app/js/views/recipes.js` ≥1 and `grep -c "_source" app/js/views/recipes.js` ≥1 (re-tag present).
    - `grep -c "cocktail" app/js/views/recipes.js` ≥1 (id convention) — id assigned as 'cocktail'+Date.now().
    - Sequential save present: `grep -c "State.save('recipes')" app/js/views/recipes.js` ≥1 and `grep -c "State.save('drafts')" app/js/views/recipes.js` ≥1, and the promote onConfirm awaits recipes BEFORE drafts (manually confirm ordering — Pitfall 4).
    - `grep -c "sameRecipe" app/js/views/recipes.js` ≥1 (dedup note) and `grep -c "WriteGate" app/js/views/recipes.js` ≥1 (gated promote write).
  </acceptance_criteria>
  <done>The Drafts tab lists draft chips; Promote-to-Original re-tags _source, assigns a cocktail<ts> id, moves the entry into originals, removes it from drafts, and saves sequentially through WriteGate with a duplicate note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: AI-13 — Claude fallback for ingredient derivation in recommender-engine.js (cached, key-gated, fail-soft)</name>
  <read_first>
    - app/js/recommender-engine.js:103-133 (DERIVATIONS table + _expandLookup) — the static map AI-13 augments
    - app/js/recommender-engine.js (the IIFE return — preserve EVERY existing export when adding `deriveWithAI`)
    - app/js/claude-api.js (07-01) — `ClaudeAPI.callMessages(body)` signature + `extractJSON` + `getKey`/`getModel`
    - .planning/phases/07-ai-integration/07-CONTEXT.md D-14 (AI-13 in scope) + 07-RESEARCH.md "Pitfall 6" (cache to avoid cost trap)
  </read_first>
  <behavior>
    - With NO API key, recommender-engine behaves EXACTLY as Phase 5 (static DERIVATIONS only): no Claude call fires, no network I/O, no `bb_derivation_cache` access. Synchronous match logic unchanged.
    - With a key set, the new `RecommenderEngine.deriveWithAI(ingredient, inventoryTokens) -> Promise<boolean>` answers whether the ingredient is reasonably derivable from the listed tokens.
    - Every decision is cached in `localStorage.bb_derivation_cache` keyed by `<ingredient.toLowerCase()>|<sortedTokens.join(',')>`; cache hit ⇒ zero network calls (Pitfall 6).
    - Fail-soft: on ANY error (no key, throw, malformed JSON, 429) the helper resolves to `false`. Never blocks the recommender; never throws upward.
    - The existing synchronous `_expandLookup` / `_hasIngredient` paths are UNCHANGED. AI-13 is an additive opt-in helper.
  </behavior>
  <action>
    1. In `app/js/recommender-engine.js`, inside the IIFE (after `DERIVATIONS`):
       - `const DERIV_CACHE_KEY = 'bb_derivation_cache';`
       - `function _loadDerivCache() { try { return JSON.parse(localStorage.getItem(DERIV_CACHE_KEY) || '{}'); } catch { return {}; } }`
       - `function _saveDerivCache(c) { try { localStorage.setItem(DERIV_CACHE_KEY, JSON.stringify(c)); } catch {} }`
       - `function _derivKey(ingredient, tokens) { return String(ingredient).toLowerCase() + '|' + [...tokens].map(String).sort().join(','); }`
    2. Add the helper (use `callMessages` + `extractJSON` directly, NOT `requestJSON` — this is a read-only query, not a write, so it sidesteps WriteGate.validate's schemaKey requirement):
       ```javascript
       async function deriveWithAI(ingredient, inventoryTokens) {
         if (typeof ClaudeAPI === 'undefined' || !ClaudeAPI.getKey || !ClaudeAPI.getKey()) return false;
         const cache = _loadDerivCache();
         const k = _derivKey(ingredient, inventoryTokens);
         if (Object.prototype.hasOwnProperty.call(cache, k)) return !!cache[k];
         try {
           const text = await ClaudeAPI.callMessages({
             model: ClaudeAPI.getModel ? ClaudeAPI.getModel() : 'claude-sonnet-4-6',
             max_tokens: 64,
             system: 'Decide whether the named ingredient is reasonably derivable from the listed inventory tokens (e.g. lemons -> lemon juice; sugar -> simple syrup; mint leaves -> muddled mint). Return JSON only: {"derived": boolean}.',
             messages: [{ role: 'user', content: 'Ingredient: ' + String(ingredient) + '\nInventory tokens: ' + [...inventoryTokens].join(', ') }],
           });
           const obj = ClaudeAPI.extractJSON(text);
           const result = !!(obj && obj.derived);
           cache[k] = result; _saveDerivCache(cache);
           return result;
         } catch (_) {
           return false; // fail-soft: never block the recommender on AI errors
         }
       }
       ```
    3. Extend the IIFE return statement to include `deriveWithAI` alongside every existing exported name (read the current return line FIRST to preserve every export — do not drop any).
    4. tests/phase-07-ai.test.js: add deterministic rows that stub `ClaudeAPI` as `{ getKey: () => 'k', getModel: () => 'claude-sonnet-4-6', callMessages: async () => '{"derived":true}', extractJSON: (t) => JSON.parse(t) }` and assert `await RecommenderEngine.deriveWithAI('lemon juice', new Set(['lemons']))` is `true` AND a second call hits the cache (`callMessages` invocation count stays at 1). Add a second row with `getKey: () => ''` asserting the helper returns `false` and `callMessages` is never called (synchronous-path preservation proof).
    DO NOT change `_expandLookup`, `DERIVATIONS`, or any existing synchronous path; AI-13 is purely additive (Phase 5 behavior preserved when no key).
  </action>
  <verify>
    <automated>node --check app/js/recommender-engine.js && node tests/phase-07-ai.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/js/recommender-engine.js` exits 0.
    - `grep -c "deriveWithAI" app/js/recommender-engine.js` returns ≥2 (definition + return-export).
    - `grep -c "bb_derivation_cache" app/js/recommender-engine.js` returns ≥1.
    - `grep -c "ClaudeAPI" app/js/recommender-engine.js` returns ≥1 (fallback present); `grep -c "streamMessage" app/js/recommender-engine.js` returns 0 (no streaming in the engine).
    - The Phase 5 recommender tests still pass (full suite green) — proves the synchronous path is unchanged.
    - The phase-07 AI-13 rows pass (cached fall-through + no-key path).
  </acceptance_criteria>
  <done>`RecommenderEngine.deriveWithAI` is exported, key-gated, cached in `bb_derivation_cache`, fail-soft, additive (Phase 5 behavior unchanged), and covered by deterministic tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>AI woven into the five existing views: Ask-Bjorn drawer entries on cards/chips (REC-04/AI-04), best-bottle advice on Inventory+Dashboard (AI-05), un-disabled Dashboard nav cards, the Wizard persona drafter (AI-12), the AI-13 Claude derivation fallback in recommender-engine.js (key-gated + cached + fail-soft), and the full AI-03 generate->draft->refine->promote flow gated by WriteGate. (Recipe balance/technique/buildability and persona voice require a live key + human judgement — per 07-VALIDATION Manual-Only table; routed to UAT.)</what-built>
  <how-to-verify>
    1. Serve: `python3 -m http.server 8000`, open http://localhost:8000/app/ with your Anthropic key set.
    2. Recommender + Recipes: click "Ask Bjorn about this" on a card and a chip → confirm the drawer opens seeded with that recipe and references your taste/mood (REC-04/AI-04).
    3. Inventory + Dashboard: click "Best bottle to add (AI)" → confirm the drawer answers with a single-bottle recommendation grounded in your inventory/vetoes (AI-05). Confirm the Dashboard "Chat with Bjorn"/"Classroom"/"Library" cards are now real links.
    4. Bartender Wizard: enter a short preference, click "Help me write this with Claude" → confirm the personality textarea fills with drafted persona text you can edit before saving (AI-12).
    5. Recipes → Generate with AI: enter "design me a spirit-forward whiskey drink" → confirm a draft is generated, schema-valid, AUTO-SAVED (appears in the Drafts tab), with a phantom-ingredient flag if it used a bottle you don't own. Confirm the refine card lets you "make it less sweet" (same draft) and "generate new" (new draft).
    6. Force an invalid generation (e.g. a nonsense prompt) → confirm it fails closed with an error and writes nothing.
    7. Drafts tab → "Promote to Original" → confirm the diff/confirm dialog, then the recipe moves to Originals with a cocktail<ts> id and disappears from Drafts (and the drafts file no longer 409s).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. drawer seeds, AI-05 advice, wizard draft, generate/auto-save, phantom flag, fail-closed, promote)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → drafts/recipes | AI-generated recipe payloads cross into the user's GitHub system of record |
| model output → DOM | drawer seeds + draft previews + wizard persona text render into the UI |
| user prompt → model | free-text design/refine prompts sent to Anthropic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-22 | Tampering | AI-03 draft / promote write | mitigate | Every drafts/recipes write goes through `WriteGate.gate` (schema-validate -> diff -> confirm); requestJSON fails closed on invalid output — no silent corruption (FM #1/#2) |
| T-07-23 | Spoofing ("buildable") | generated recipe ingredients | mitigate | `WriteGate.inventoryFidelity` flags phantom/veto'd ingredients in the draft preview before promote (FM #3 / 1b buildability) |
| T-07-24 | Tampering (XSS) | draft preview / refine card / persona text | mitigate | All model output escaped via `Utils.escapeHtml` before innerHTML (textarea value assignment is safe; rendered chips/previews escaped) — Security V10 |
| T-07-25 | DoS (cost) | repeated generate/refine calls | mitigate | max_tokens cap on every requestJSON body; drawer reuse means no extra streaming controllers; no auto-retry beyond requestJSON's single retry |
| T-07-26 | Tampering | promote 409 / partial write | mitigate | Sequential State.save (recipes then drafts) per Pitfall 4; existing 409-refetch-retry covers conflicts; duplicate flagged via Utils.sameRecipe before confirm |
| T-07-27 | Tampering | wizard persona draft auto-write | mitigate | AI-12 only fills the textarea; persistence still goes through the existing user-edit + State.patch save — no direct AI->GitHub write |
</threat_model>

<verification>
- `node --check` passes on all five view files.
- grep gates in Tasks 1-3 pass; every drafts/recipes AI write is inside a WriteGate.gate.
- Full suite stays green: `for f in tests/*.test.js; do node "$f" || exit 1; done` (07-01's inventory-fidelity / write-gate / drafts-normalize rows cover the deterministic side; recipe quality is UAT).
- Human-verify checkpoint (Task 4) covers the live-key behaviors.
</verification>

<success_criteria>
Bjorn is reachable from every recipe card/chip and from Inventory/Dashboard (REC-04/AI-04/AI-05), the Dashboard cards are live, the Wizard can draft persona text (AI-12), and AI cocktail design produces fail-closed, schema-valid, phantom-flagged drafts that can be refined and promoted to Originals through WriteGate (AI-03). All AI writes are gated.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-integration/07-05-SUMMARY.md`
</output>
