---
phase: 07-ai-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/js/claude-api.js
  - app/js/state.js
  - app/js/normalize.js
  - app/js/write-gate.js
  - schema/drafts.schema.json
  - data/drafts.json
  - app/index.html
  - tests/phase-07-ai.test.js
autonomous: true
requirements: [CHAT-02, CHAT-05, CHAT-08, CHAT-09, AI-13]
must_haves:
  truths:
    - "ClaudeAPI exposes streamMessage, requestJSON, callMessages, buildContext, deriveContextMarkdown alongside the still-working generateRecipe"
    - "getModel() returns claude-sonnet-4-6 when no bb_chat_model override is set (stale 4-5 ID removed)"
    - "appendLog caps bb_api_log at 50 entries and never records the API key"
    - "drafts is registered as a 5th State data file; loadAll tolerates a missing data/drafts.json (treats 404 as empty)"
    - "Normalize.drafts is idempotent, tags each draft _source:'ai-generated', and is dispatched by byKey('drafts')"
    - "write-gate.js validates a payload against schema/<key>.schema.json and blocks (no write offered) when invalid"
    - "node tests/phase-07-ai.test.js passes"
  artifacts:
    - path: "app/js/claude-api.js"
      provides: "streaming, structured-JSON, context builder, hardened log, correct default model"
      contains: "streamMessage"
    - path: "app/js/state.js"
      provides: "drafts registered in FILES + tolerant loadAll"
      contains: "drafts:"
    - path: "app/js/normalize.js"
      provides: "Normalize.drafts + byKey dispatch"
      contains: "function drafts"
    - path: "app/js/write-gate.js"
      provides: "reusable schema-validate -> diff -> confirm -> write gate"
      contains: "WriteGate"
    - path: "schema/drafts.schema.json"
      provides: "drafts file schema (savedRecipe base + draft fields)"
      contains: "ai-generated"
    - path: "data/drafts.json"
      provides: "committed seed so existing repos do not 404"
      contains: "drafts"
    - path: "tests/phase-07-ai.test.js"
      provides: "deterministic node:test coverage for foundation"
      contains: "getModel"
  key_links:
    - from: "app/js/state.js"
      to: "data/drafts.json"
      via: "FILES.drafts + tolerant loadAll"
      pattern: "drafts.*data/drafts.json"
    - from: "app/js/state.js"
      to: "Normalize.drafts"
      via: "_normalize('drafts', data) -> byKey"
      pattern: "drafts"
    - from: "app/js/write-gate.js"
      to: "schema/drafts.schema.json"
      via: "validateAgainstSchema(schemaKey)"
      pattern: "schema"
    - from: "app/index.html"
      to: "app/js/write-gate.js"
      via: "<script> tag after claude-api.js"
      pattern: "write-gate.js"
---

<objective>
Build the AI foundation every other Phase 7 plan consumes: extend `claude-api.js` with streaming (CHAT-05/08/09), a structured-JSON helper (`requestJSON`), a non-streaming primitive (`callMessages`), the rich cached-context builder (D-04/D-05) with JSON→MD derivation (D-06); register `drafts` as the 5th State data file (D-11) with its schema, seed, and normalizer; create the reusable schema→diff→confirm write-gate (Pattern 3); and stand up the `node:test` harness with fixtures. Also fix the stale `DEFAULT_MODEL` and harden `appendLog` (AI-09 key hygiene).

Purpose: This is Wave 1. Plans 02–06 cannot stream, generate structured JSON, persist drafts, or safely write AI output to GitHub without these primitives. Building them once (one chokepoint, one write-gate, one normalizer) prevents three places to get key hygiene and silent-corruption wrong.
Output: Extended `claude-api.js`, `state.js`, `normalize.js`; new `write-gate.js`, `schema/drafts.schema.json`, `data/drafts.json`, `tests/phase-07-ai.test.js`; `<script>` wiring in `index.html`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-integration/07-AI-SPEC.md
@.planning/phases/07-ai-integration/07-RESEARCH.md
@.planning/phases/07-ai-integration/07-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Extracted from the codebase. Use directly — do not re-explore. -->

Existing claude-api.js (app/js/claude-api.js) — EXTEND, keep generateRecipe working:
```javascript
const ClaudeAPI = (() => {
  const ENDPOINT      = 'https://api.anthropic.com/v1/messages';
  const API_VERSION   = '2023-06-01';
  const DEFAULT_MODEL = 'claude-sonnet-4-5-20250929';   // STALE -> 'claude-sonnet-4-6'
  function getKey()   { return localStorage.getItem('bb_anthropic_key') || ''; }
  function getModel() { return localStorage.getItem('bb_chat_model') || DEFAULT_MODEL; }
  const LOG_KEY = 'bb_api_log'; const LOG_MAX = 50;
  function appendLog(entry) { /* 50-entry splice; current callers log prompt/system/raw text */ }
  function extractJSON(text) { /* strips ```json fences then JSON.parse */ }
  async function generateRecipe(userPrompt, ctx) { /* non-streaming; reuses header block below */ }
  return { generateRecipe };           // ADD new functions here
})();
```
Header block to REUSE verbatim for every new call (claude-api.js:87-93):
```javascript
headers: {
  'content-type': 'application/json',
  'x-api-key': key,
  'anthropic-version': API_VERSION,
  'anthropic-dangerous-direct-browser-access': 'true',   // CHAT-02 mandatory
}
```

Existing state.js FILES (app/js/state.js:4-9), loadAll Promise.all (state.js:41-50), save+409-retry (state.js:71-99), set->_normalize (state.js:103-107):
```javascript
const FILES = { barkeeper:'data/barkeeper.json', profile:'data/bar-owner-profile.json',
                inventory:'data/inventory.json', recipes:'data/recipes.json' };  // ADD drafts
```

Existing normalize.js byKey (normalize.js:251-257), recipes() analog (normalize.js:233-248), helpers ensureObject/ensureArray/ensureString/isoToday (normalize.js:7-10).

Existing schema/recipes.schema.json savedRecipe def (recipes.schema.json:134-152) carries _source "classics-db | originals | ai-generated".

Existing write-gate analog: export.js:264-328 (renderPreview rows + #imp-confirm handler + sequential State.set/State.save). export.js:56-145 exportAIContext (JSON->MD walk) is the deriveContextMarkdown precedent.

Test harness boilerplate to COPY (tests/phase-06-engine.test.js:9-24):
```javascript
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs'), path = require('node:path'), vm = require('node:vm');
global.window = {};
global.localStorage = { _s:{}, getItem(k){return this._s[k]??null;}, setItem(k,v){this._s[k]=String(v);}, removeItem(k){delete this._s[k];} };
global.document = { getElementById: () => null };
vm.runInThisContext(fs.readFileSync(path.resolve(__dirname, '../app/js/normalize.js'), 'utf8'));
```

Model IDs (AI-SPEC §4, re-verified): Haiku claude-haiku-4-5, Sonnet (default) claude-sonnet-4-6, Opus claude-opus-4-7. Use bare aliases, no date suffix. Do NOT send temperature/top_p/top_k/thinking/effort.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend claude-api.js — streaming, requestJSON, callMessages, context builder, model fix, log hardening</name>
  <read_first>
    - app/js/claude-api.js (the full module being extended)
    - .planning/phases/07-ai-integration/07-AI-SPEC.md §3 (streamMessage SSE reader) + §4 (model IDs, max_tokens, no sampling params) + §4b (requestJSON contract)
    - .planning/phases/07-ai-integration/07-RESEARCH.md "Pattern 1" + "Code Examples" (pre-written streamMessage + requestJSON bodies — copy, do not invent)
    - app/js/export.js:56-145 (exportAIContext — JSON->MD derivation analog for deriveContextMarkdown)
  </read_first>
  <behavior>
    - getModel() returns 'claude-sonnet-4-6' when localStorage.bb_chat_model is unset; returns the override when set (SET-05).
    - appendLog({type:'chat',model,usage}) writes an entry with NO 'x-api-key'/'key'/'prompt'/'system' field; after 60 appends, bb_api_log length === 50 (oldest dropped).
    - extractJSON tolerates a ```json-fenced object and a clean object; throws on non-JSON.
    - requestJSON returns the Normalized object when the model returns valid JSON; throws (caller must NOT write) after one failed retry on unparseable/invalid output.
    - buildContext() returns an array of system text blocks whose LAST block carries cache_control:{type:'ephemeral'} and contains NO volatile value (no timestamp/UUID/question).
  </behavior>
  <action>
    In app/js/claude-api.js:
    1. Change `DEFAULT_MODEL = 'claude-sonnet-4-5-20250929'` to `'claude-sonnet-4-6'` (per AI-SPEC §4; remove the stale ID and its re-verify comment, replace with a comment listing the three valid IDs claude-haiku-4-5 / claude-sonnet-4-6 / claude-opus-4-7).
    2. Add `function callMessages(body)` — non-streaming POST to ENDPOINT using the reused header block (key from getKey()); replicate the existing 401/429/529 error mapping (claude-api.js:96-105) including `retry-after` on 429 (CHAT-09); on success return `data.content?.[0]?.text || ''`; call `appendLog({type: body._logType||'request', model: body.model, usage: data.usage})` (NEVER log key/prompt/system).
    3. Add `async function streamMessage(body, { onText, signal })` per AI-SPEC §3 / RESEARCH Pattern 1 verbatim: fetch with `{...body, stream:true}` and `signal`; on 429 throw `Rate limited — retry after ${resp.headers.get('retry-after')||'?'}s.` (CHAT-09, no retry loop); buffer-split SSE on '\n' keeping the trailing partial; on `content_block_delta`+`text_delta` call `onText(evt.delta.text)`; on `message_delta` capture `evt.usage`; on `error` throw `evt.error?.message` (CHAT-08); finally `appendLog({type:'chat', model:body.model, usage})` and return usage.
    4. Add `async function requestJSON({system, userPrompt, schemaKey, model, maxTokens})` per AI-SPEC §4b / RESEARCH Code Examples verbatim: JSON-only instruction appended to system; parse via existing `extractJSON`; `Normalize.byKey(schemaKey, obj)`; validate via `WriteGate.validate(schemaKey, norm)` (from Task 3) if available else a local shape check; ONE retry with the error string; throw StructuredOutputError-style Error on second failure (fail-closed). Default `maxTokens` 1024.
    5. Add `function deriveContextMarkdown()` (D-06): walk `State._data` (guard if State undefined) and emit readable markdown for persona (barkeeper), flavor profile (bar-owner-profile), current mood (RecommenderEngine.normalizeProfile if available), originals (recipes.originals), drafts (drafts.drafts), made-log with times_made (recipes.made_log), and inventory + vetoes. Reuse the `# / ## / -` walk style of export.js exportAIContext. Add a top comment: "context derivation, lossy-OK — NOT the Phase 8 round-trippable export contract".
    6. Add `function buildContext()` (D-04/D-05): return `[ {type:'text', text: BASE_PERSONA_INSTRUCTION}, {type:'text', text: deriveContextMarkdown(), cache_control:{type:'ephemeral'}} ]`. Never interpolate the question/timestamp into either block (Pitfall 5/6).
    7. Harden `appendLog`: keep the 50-cap splice; ensure no caller passes the key. Update generateRecipe's appendLog calls to drop `prompt`/`system`/`raw` text (log `{type,model,usage}` only) so old verbose entries are superseded.
    8. Export all new functions: `return { generateRecipe, callMessages, streamMessage, requestJSON, buildContext, deriveContextMarkdown, appendLog, getKey, getModel, extractJSON };`
    Do NOT send temperature/top_p/top_k/thinking/effort in any body (AI-SPEC §4 — rejected on some tiers).
  </action>
  <verify>
    <automated>node -e "require('node:vm').runInThisContext(require('node:fs').readFileSync('app/js/claude-api.js','utf8')); ['streamMessage','requestJSON','callMessages','buildContext','deriveContextMarkdown'].forEach(f=>{if(typeof ClaudeAPI[f]!=='function')throw new Error('missing '+f)}); console.log('ok')" </automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/js/claude-api.js` exits 0.
    - `grep -c "claude-sonnet-4-5-20250929" app/js/claude-api.js` returns 0.
    - `grep -c "claude-sonnet-4-6" app/js/claude-api.js` returns ≥1.
    - `grep -c "anthropic-dangerous-direct-browser-access" app/js/claude-api.js` returns ≥2 (generateRecipe + new calls).
    - `grep -c "cache_control" app/js/claude-api.js` returns ≥1.
    - The verify command prints `ok` (all 5 new functions exported).
    - No `appendLog` call site passes a property named `key`, `prompt`, `system`, or `raw` (grep: `grep -n "appendLog(" app/js/claude-api.js` — manually confirm none include those keys).
  </acceptance_criteria>
  <done>streamMessage/requestJSON/callMessages/buildContext/deriveContextMarkdown exported and syntactically valid; default model is claude-sonnet-4-6; browser-direct header present in new calls; cache_control on last context block; key never logged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register drafts as 5th State file + Normalize.drafts + schema + committed seed</name>
  <read_first>
    - app/js/state.js (FILES at :4-9, loadAll at :41-50, save at :71-99, set/_normalize at :103-107)
    - app/js/normalize.js (recipes() at :233-248, byKey at :251-257, helpers at :7-10)
    - schema/recipes.schema.json (savedRecipe def at :134-152, id pattern ^cocktail[0-9]+$ at :59)
    - .planning/phases/07-ai-integration/07-RESEARCH.md "Pattern 4" + "Pitfall 1" (tolerant 404 load)
  </read_first>
  <behavior>
    - Normalize.byKey('drafts', {}) returns `{ drafts:[], last_updated:<isoToday> }`.
    - Normalize.drafts is idempotent: byKey('drafts', byKey('drafts', x)) deep-equals byKey('drafts', x).
    - Each entry in the returned drafts[] carries _source:'ai-generated'.
    - State.loadAll() treats a 404 on data/drafts.json as `{drafts:[]}` (does not reject the whole load).
  </behavior>
  <action>
    1. schema/drafts.schema.json — create mirroring schema/recipes.schema.json: top-level `{ drafts: array of draftItem, last_updated: string }`. draftItem reuses the savedRecipe field set (name required; base, method, glassware, garnish, occasion, ingredients[], notes, tasting_notes, why_it_works) plus `_source` (const-ish "ai-generated"), `draft_id`, `created_at`, `updated_at`, `source_prompt`. Copy the `ingredient` definition from recipes.schema.json.
    2. data/drafts.json — create and COMMIT the seed exactly: `{ "drafts": [], "last_updated": "2026-05-21" }` (Pitfall 1: existing repos 404 without it).
    3. app/js/normalize.js — add `function drafts(data)` in the shape of `recipes()`: ensureObject(data); ensure `drafts` is an array (ensureArray); for each entry set `_source = 'ai-generated'`, ensure `name` string, drop unknown keys not in the draftItem field set, preserve draft_id/created_at/updated_at/source_prompt; set `last_updated = data.last_updated || isoToday()`; idempotent. Register in byKey: add `if (key === 'drafts') return drafts(data);`. Export `drafts` if the IIFE returns named functions (match how recipes is exposed).
    4. app/js/state.js — add `drafts: 'data/drafts.json'` to FILES; update the `_data`/`_shas` comments to include drafts. In loadAll, make the per-file read tolerant: wrap the `drafts` read so a 404/Not Found resolves to `{ data:{drafts:[]}, sha:null }` instead of rejecting (keep other files strict). save('drafts') and set('drafts') need no change (FILES-driven).
    5. app/index.html — no new tag needed for state/normalize (already loaded); confirm load order unchanged.
  </action>
  <verify>
    <automated>node tests/phase-07-ai.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/js/state.js` and `node --check app/js/normalize.js` exit 0.
    - `grep -c "drafts" app/js/state.js` returns ≥1 (FILES entry present).
    - `grep -c "function drafts" app/js/normalize.js` returns 1.
    - `grep -c "key === 'drafts'" app/js/normalize.js` returns 1.
    - `data/drafts.json` parses as JSON and has a `drafts` array: `node -e "const d=require('./data/drafts.json'); if(!Array.isArray(d.drafts))throw 0; console.log('ok')"` prints ok.
    - `schema/drafts.schema.json` parses as JSON and contains the string `ai-generated`.
    - The phase-07 test's drafts-normalize + idempotency rows pass.
  </acceptance_criteria>
  <done>drafts is the 5th FILES entry; loadAll tolerant of missing file; Normalize.drafts idempotent + _source-tagging; schema + committed seed exist and parse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reusable write-gate.js + test harness with fixtures</name>
  <read_first>
    - app/js/export.js:264-328 (renderPreview rows + #imp-confirm + sequential State.set/State.save — the write-gate skeleton)
    - app/js/state.js:71-99 (save + 409 refetch-retry the gate calls into)
    - tests/phase-06-engine.test.js:9-24 (node:test harness boilerplate to copy)
    - .planning/phases/07-ai-integration/07-RESEARCH.md "Pattern 3" + "Validation Architecture" (Wave 0 gaps, fixtures list)
    - .planning/phases/07-ai-integration/07-AI-SPEC.md §5 (reference dataset / fixture composition) + §6 (guardrail: schema-validate -> diff -> confirm before write)
  </read_first>
  <behavior>
    - WriteGate.validate('drafts', validPayload) returns [] (no errors); WriteGate.validate('drafts', {missing name}) returns a non-empty error array.
    - WriteGate.gate(...) NEVER calls State.save when validate returns errors (fail-closed).
    - Inventory-fidelity helper flags an ingredient absent from the fixture inventory token set (phantom) and passes one present in it.
  </behavior>
  <action>
    1. app/js/write-gate.js — create `const WriteGate = (() => { ... })();` exposing:
       - `validate(schemaKey, payload)` — a lightweight shape validator over `schema/<key>.schema.json` (the app has no JSON-Schema runtime): in the browser, validate against the relevant schema's required fields/shape; expose a pure function that takes the schema object so node tests can pass the schema in. Return an array of human-readable error strings ([] = valid).
       - `async gate({ schemaKey, oldData, newPayload, message, onConfirm })` — coerce via `Normalize.byKey`; call `validate`; if errors, surface them and RETURN WITHOUT offering a write (fail-closed, FM #1/#2); if valid, render an old-vs-new diff preview (reuse export.js renderPreview row style) into a `.confirm-dialog-overlay` (settings.js:41-59 pattern) and wire a confirm button that runs `onConfirm()` (which does State.set/patch + sequential `await State.save(...)`). Backdrop/cancel closes without writing.
       - `inventoryFidelity(recipe, inventoryTokens, vetoes)` — return `{ phantoms:[...], vetoed:[...] }`: ingredient names not in inventoryTokens (and not flagged as substitution) are phantoms; veto'd ingredient names land in vetoed. Reuse the recommender's token-normalization approach (lowercase membership).
    2. app/index.html — add `<script src="js/write-gate.js"></script>` AFTER claude-api.js (line ~102) and BEFORE app.js (line ~105).
    3. tests/phase-07-ai.test.js — create using the phase-06 harness. Load normalize.js, claude-api.js, write-gate.js via vm.runInThisContext with stubbed globals. Cover ALL Wave-0 Code rows: getModel default===claude-sonnet-4-6 + honors override; appendLog caps at 50 and entry has no key field; extractJSON parses fenced + clean, throws on garbage; Normalize.drafts idempotent + _source tag; promote re-tag id matches ^cocktail[0-9]+$ (helper-level); WriteGate.validate returns errors for an invalid drafts payload and [] for a valid one; inventoryFidelity flags a phantom against a fixture inventory. Inline-define minimal fixtures (valid recipe payload, invalid payload missing name, a small inventory token set); add a `tests/fixtures/` note in a comment for AI-08/AI-10/AI-11 blobs to be added by their plans.
  </action>
  <verify>
    <automated>node tests/phase-07-ai.test.js && for f in tests/*.test.js; do node "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/js/write-gate.js` exits 0.
    - `node tests/phase-07-ai.test.js` exits 0 and reports the rows: getModel, appendLog-cap, extractJSON, drafts-normalize, write-gate-validate, inventory-fidelity.
    - Full suite green: `for f in tests/*.test.js; do node "$f" || exit 1; done` exits 0 (Phase 5/6 regression intact).
    - `grep -c "write-gate.js" app/index.html` returns 1.
    - `grep -v '^[[:space:]]*//' app/js/write-gate.js | grep -c "State.save"` returns ≥1 (write only after confirm).
  </acceptance_criteria>
  <done>WriteGate.validate/gate/inventoryFidelity exist and are script-wired; invalid payloads never reach State.save; phase-07 test green and full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → api.anthropic.com | BYOK key crosses here in `x-api-key`; model output (untrusted) crosses back |
| AI output → State/GitHub | parsed/generated payloads cross into the user's system of record |
| localStorage → app | bb_anthropic_key, bb_chat_model, bb_api_log read by claude-api.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Info Disclosure | appendLog / bb_api_log | mitigate | appendLog records only {ts,type,model,usage}; Task 1 strips prompt/system/raw and never includes the key (FM #4); test asserts no key field |
| T-07-02 | Info Disclosure | streamMessage/callMessages headers | mitigate | key sent only in x-api-key to ENDPOINT (api.anthropic.com) with browser-direct header; no other host |
| T-07-03 | Tampering | requestJSON / WriteGate.validate | mitigate | parse → Normalize → schema-validate → one retry → fail-closed; invalid payload never offered for write (FM #1/#2) |
| T-07-04 | DoS (user wallet) | streamMessage on 429 | mitigate | surface retry-after, no auto-retry loop (CHAT-09); max_tokens cap on every body (FM #5) |
| T-07-05 | Tampering | deriveContextMarkdown cached block | accept | context is read-only derivation of the user's own data; no external input mixed in; lossy-OK |
| T-07-06 | Spoofing ("buildable") | inventoryFidelity helper | mitigate | helper flags phantom/veto'd ingredients so downstream plans can surface them (FM #3) |
</threat_model>

<verification>
- `node --check` passes on claude-api.js, state.js, normalize.js, write-gate.js.
- `node tests/phase-07-ai.test.js` green; full suite `for f in tests/*.test.js; do node "$f" || exit 1; done` green.
- data/drafts.json and schema/drafts.schema.json parse as JSON.
- grep gates in each task's acceptance_criteria all pass.
</verification>

<success_criteria>
ClaudeAPI exposes streaming + structured-JSON + context builder with the correct default model and key-safe log; drafts is a working 5th State file with schema, seed, and idempotent normalizer; WriteGate blocks invalid payloads; the deterministic test suite is green. Plans 02–06 can now build on these primitives.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-integration/07-01-SUMMARY.md`
</output>
