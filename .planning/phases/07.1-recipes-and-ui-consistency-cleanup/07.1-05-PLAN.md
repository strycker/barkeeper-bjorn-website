---
phase: 07.1-recipes-and-ui-consistency-cleanup
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - app/css/app.css
  - app/js/views/recipes.js
  - app/js/views/settings.js
autonomous: true
requirements: [UI-TOKEN-02]
must_haves:
  truths:
    - ":root defines a --space-* spacing scale"
    - "The named utility/section classes .form-section-card, .form-actions-row, .muted-help, .input-disabled exist in app.css and are used in recipes.js and settings.js"
    - "Inline style= attribute counts in recipes.js and settings.js drop substantially below their current baselines (recipes.js 87, settings.js 38)"
    - "node --check passes on recipes.js and settings.js and the full deterministic test suite is green"
  artifacts:
    - path: "app/css/app.css"
      provides: "--space-* tokens + .form-section-card / .form-actions-row / .muted-help / .input-disabled"
      contains: "--space-"
    - path: "app/js/views/recipes.js"
      provides: "form section/action/help/disabled markup via named classes"
    - path: "app/js/views/settings.js"
      provides: "section/help markup via named classes"
  key_links:
    - from: ".form-section-card / .form-actions-row / .muted-help / .input-disabled usages"
      to: "app.css class definitions"
      via: "class= attribute references"
      pattern: "form-section-card|form-actions-row|muted-help|input-disabled"
    - from: "app.css spacing declarations"
      to: ":root --space-* tokens"
      via: "padding/margin/gap: var(--space-*)"
      pattern: "var\\(--space-"
---

<objective>
Add a spacing token scale and extract the highest-frequency inline `style=` blocks in
recipes.js (87) and settings.js (38) into named utility/section classes (UI-TOKEN-02). The
Phase 7 UI Review scored Spacing 2/4 and called the inline-style sprawl "the largest source
of styling drift," violating the CLAUDE.md convention that all visual decisions live in
app/css/app.css.

This is the LAST plan in the phase (Wave 4) so the recipes.js structural work from CHIP-04
(plan 02) and RECIPE-GEN-01 (plan 03) has already landed — extracting styles last avoids
edit collisions on recipes.js. It also carries the phase-wide final verification task.

Purpose: One place to change spacing/section framing; future audits grep one selector.

Output: --space-* tokens in :root; the four named classes the UI Review prescribed
(.form-section-card, .form-actions-row, .muted-help, .input-disabled) defined and applied;
inline style counts substantially reduced; full suite green.

DESIGN CONTRACT: No formal UI-SPEC.md. The Phase 7 UI Review's Top fix #2 (07-UI-REVIEW.md)
is the design contract — it names the exact four classes and the inline patterns to extract.

REALITY NOTE: current inline counts verified — recipes.js = 87, settings.js = 38. The four
target classes do NOT yet exist in app.css (grep confirmed zero). "Substantially below"
threshold: recipes.js < 60, settings.js < 25 (a meaningful reduction covering the repeated
section-card / actions-row / muted-help / disabled patterns; full elimination is not
required and some one-off inline styles may legitimately remain).
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ai-integration/07-UI-REVIEW.md
@CLAUDE.md

<interfaces>
<!-- :root already has (after plan 04) --fs-* tokens, plus --radius/--radius-sm/--shadow/
     --transition. Add --space-* in the same block. -->

<!-- Spacing scale to define (4px base step): -->
  --space-1: 4px;  --space-2: 8px;  --space-3: 12px;  --space-4: 16px;
  --space-5: 20px; --space-6: 24px;

<!-- Four named classes to define (from UI Review Top fix #2). Suggested rule bodies —
     match existing inline values so visuals don't shift:
  .form-section-card { margin-bottom: var(--space-5); padding: var(--space-3);
    background: var(--bg3); border: 1px solid var(--border); border-radius: var(--radius-sm); }
  .form-actions-row  { display: flex; gap: var(--space-3); margin-top: var(--space-5); flex-wrap: wrap; }
  .muted-help        { font-size: var(--fs-sm); color: var(--text-dim); }
  .input-disabled    { opacity: 0.5; cursor: not-allowed; } -->

<!-- Recurring inline patterns to replace (from UI Review, verified in source):
  recipes.js ~1539/1519: section-card framing  → class="form-section-card"
  recipes.js ~1645: form-actions footer        → class="form-actions-row"
  recipes.js ~1633 (x4 checkbox rows): display:flex;align-items:center;gap:10px → a .form-check-row class (optional 5th) OR keep
  recipes.js ~1641: disabled checkbox inline opacity/cursor → class="input-disabled"
  settings.js ~205/190/143 (x6+): font-size:0.82rem;color:var(--text-dim) → class="muted-help"
  settings.js ~236: sub-panel framing margin-top:24px;padding-top:16px;border-top → a .settings-subpanel class (optional) OR form-section-card -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add --space-* tokens and the four named utility/section classes to app.css</name>
  <read_first>
    - app/css/app.css (lines 4-24 :root; the --fs-* block added by plan 04; existing .form-group / .form-row / .btn rules so new classes sit consistently)
    - .planning/phases/07-ai-integration/07-UI-REVIEW.md (Top fix #2 + Pillar 5 detail)
  </read_first>
  <action>
    1. In :root (app/css/app.css), after the --fs-* block (added by plan 04) and before
       --shadow, insert the spacing scale:
         /* Spacing scale (UI-TOKEN-02) — 4px base step. */
         --space-1: 4px;
         --space-2: 8px;
         --space-3: 12px;
         --space-4: 16px;
         --space-5: 20px;
         --space-6: 24px;

    2. Add the four named classes near the existing form rules (search for `.form-group`
       and place these adjacent). Use the spacing + type tokens so values match the inline
       blocks they replace:
         .form-section-card {
           margin-bottom: var(--space-5);
           padding: var(--space-3);
           background: var(--bg3);
           border: 1px solid var(--border);
           border-radius: var(--radius-sm);
         }
         .form-actions-row {
           display: flex;
           gap: var(--space-3);
           margin-top: var(--space-5);
           flex-wrap: wrap;
         }
         .muted-help {
           font-size: var(--fs-sm);
           color: var(--text-dim);
         }
         .input-disabled {
           opacity: 0.5;
           cursor: not-allowed;
         }
  </action>
  <verify>
    <automated>test $(grep -cE '^\s*--space-[1-6]:' app/css/app.css) -eq 6 && echo SPACE-TOKENS && grep -c -E '\.form-section-card|\.form-actions-row|\.muted-help|\.input-disabled' app/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE '^\s*--space-[1-6]:' app/css/app.css returns 6
    - app.css contains rule definitions for .form-section-card, .form-actions-row, .muted-help, .input-disabled (grep each — 4 selectors present)
    - At least 4 declarations in these new rules use var(--space-*) or var(--fs-*)
  </acceptance_criteria>
  <done>Spacing tokens and the four named classes exist in app.css.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace repeated inline style blocks in recipes.js and settings.js with the named classes</name>
  <read_first>
    - app/js/views/recipes.js (lines 1505-1670 the renderForm section cards, form-actions footer, checkbox rows, disabled input; grep all `style=` to map repeats)
    - app/js/views/settings.js (lines 125-240 — repeated muted-help text + sub-panel framing; grep all `style=`)
    - app/css/app.css (the four classes added in Task 1 — confirm class names)
  </read_first>
  <action>
    Replace the recurring inline patterns with the named classes. Do NOT rewrite the whole
    files — target the repeated, prescribed patterns. Concrete replacements:

    recipes.js:
    - The two "section card" wrappers in renderForm (the `id="rf-ai-wrap"` block at ~1538
      and, if still present after plan 03, any sibling section-card div, plus the isDraft
      `id="rf-tweak-wrap"` block at ~1519): replace the inline
        style="margin-bottom:20px;padding:12px;background:var(--bg3);border:1px solid var(--border);border-radius:var(--radius-sm);"
      with class="rf-ai-prompt-wrap form-section-card" (keep any existing class, append the new one).
    - The form-actions footer at ~1645:
        style="display:flex;gap:10px;margin-top:20px;flex-wrap:wrap;"
      → class="form-actions-row" (remove the inline style).
    - The disabled-checkbox inline at ~1641 (`${r?.seed_id ? 'opacity:0.5;cursor:not-allowed;' : ''}` inside an inline style):
      move the disabled styling to a conditional class: add class="${r?.seed_id ? 'input-disabled' : ''}"
      to that input and drop the opacity/cursor from its inline style (keep width:auto;margin:0 if needed
      or move those into a small rule too — minimum: remove the opacity/cursor inline).

    settings.js:
    - The repeated status/help paragraphs using
        style="font-size:0.82rem;color:var(--text-dim);..." (and the `min-height:1.2em;margin-top:8px;font-size:0.82rem` status lines)
      → add class="muted-help" and remove the font-size+color from the inline style (keep
      min-height/margin-top inline only where structurally needed, or leave them — the goal is
      to remove the repeated font-size:0.82rem;color:var(--text-dim) pairs). Apply to the 6+
      occurrences (lines ~133, ~166, ~190, ~194, ~205, ~208, ~212, ~215 region — grep to find all).
    - The sub-panel framing at ~236 (margin-top:24px;padding-top:16px;border-top:1px solid var(--amber-dim)):
      this is a distinct pattern; leave it inline OR (preferred) add a `.settings-subpanel`
      rule. Not required for the threshold — focus on muted-help count reduction.

    After edits, grep-count inline `style=` in both files and confirm the thresholds are met.
    If recipes.js is still >= 60, extract one more repeated pattern (the checkbox rows at
    ~1633 `display:flex;align-items:center;gap:10px` appear ~4×) into a `.form-check-row`
    class (add it to app.css alongside the others) and apply it.
  </action>
  <verify>
    <automated>node --check app/js/views/recipes.js && node --check app/js/views/settings.js && R=$(grep -o 'style=' app/js/views/recipes.js | wc -l) && S=$(grep -o 'style=' app/js/views/settings.js | wc -l) && echo "recipes=$R settings=$S" && test "$R" -lt 60 && test "$S" -lt 25 && echo THRESHOLDS-MET</automated>
  </verify>
  <acceptance_criteria>
    - inline `style=` count in app/js/views/recipes.js is < 60 (baseline 87)
    - inline `style=` count in app/js/views/settings.js is < 25 (baseline 38)
    - grep -c 'form-section-card' app/js/views/recipes.js is >= 1
    - grep -c 'form-actions-row' app/js/views/recipes.js is >= 1
    - grep -c 'input-disabled' app/js/views/recipes.js is >= 1
    - grep -c 'muted-help' app/js/views/settings.js is >= 4
    - node --check passes on both files
  </acceptance_criteria>
  <done>Repeated inline section/actions/help/disabled styling in recipes.js and settings.js is replaced by the named classes; inline counts dropped below threshold.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase-wide final verification — full suite + node --check sweep</name>
  <read_first>
    - tests/ (all *.test.js)
    - .planning/ROADMAP.md (Phase 7.1 success criteria 1-5)
  </read_first>
  <action>
    Run the phase-wide verification gate confirming all five requirements landed cleanly:
    1. node --check every JS file touched across the phase:
       app/js/views/recipes.js, app/js/views/recommender.js, app/js/views/dashboard.js,
       app/js/views/profile.js, app/js/views/settings.js, app/js/export.js,
       app/js/claude-api.js, app/js/state.js.
    2. Run every deterministic test: node tests/phase-05-engine.test.js,
       tests/phase-05-normalize.test.js, tests/phase-06-engine.test.js,
       tests/phase-07-ai.test.js — all must report `# fail 0`.
    3. Cross-requirement greps confirming the phase outcomes:
       - CHIP-03: grep -rnE "State\.get\('recipes'\)\?\.(originals|confirmed_favorites|wishlist|made_log)" app/js/ → 0; grep -c "_withRecipesShim" app/js/state.js → 0
       - RECIPE-GEN-01: grep -c "Create Recipe" app/js/views/recipes.js → 0
       - UI-TOKEN-01: grep -cE '^\s*--fs-' app/css/app.css → >= 6
       - UI-TOKEN-02: grep -cE '^\s*--space-' app/css/app.css → >= 6
    If anything fails, fix the offending file (this task is the safety net). Record the final
    numbers in the SUMMARY.
  </action>
  <verify>
    <automated>for f in app/js/views/recipes.js app/js/views/recommender.js app/js/views/dashboard.js app/js/views/profile.js app/js/views/settings.js app/js/export.js app/js/claude-api.js app/js/state.js; do node --check "$f" || exit 1; done && for t in tests/*.test.js; do node "$t" || exit 1; done && test $(grep -rcE "State\.get\('recipes'\)\?\.(originals|confirmed_favorites|wishlist|made_log)" app/js/ | awk -F: '{s+=$2} END{print s+0}') -eq 0 && test $(grep -c "Create Recipe" app/js/views/recipes.js) -eq 0 && echo PHASE-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - node --check exits 0 on all 8 listed files
    - all four test files report `# fail 0`
    - grep -rnE "State\.get\('recipes'\)\?\.(originals|confirmed_favorites|wishlist|made_log)" app/js/ → 0 matches
    - grep -c "_withRecipesShim" app/js/state.js → 0
    - grep -c "Create Recipe" app/js/views/recipes.js → 0
    - grep -cE '^\s*--fs-' app/css/app.css → >= 6 ; grep -cE '^\s*--space-' app/css/app.css → >= 6
  </acceptance_criteria>
  <done>All five phase requirements verified clean: full deterministic suite green, node --check clean, cross-requirement greps satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | CSS class extraction + token addition; markup attribute changes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-075-01 | Tampering/XSS | recipes.js / settings.js markup edits | accept | Moving inline style strings to class= attributes does not alter any data flow or interpolation; all existing Utils.escapeHtml sinks are untouched. Class names are static literals, not user-derived. |
| T-075-02 | (n/a) | app.css token/class additions | accept | Pure stylesheet additions; no script/input surface. |

No NEW threat introduced — token addition + inline-to-class extraction only. Stated explicitly per the phase security gate.
</threat_model>

<verification>
- `grep -cE '^\s*--space-[1-6]:' app/css/app.css` → 6
- `grep -o 'style=' app/js/views/recipes.js | wc -l` → < 60
- `grep -o 'style=' app/js/views/settings.js | wc -l` → < 25
- four named classes defined in app.css and referenced in the JS views
- `node tests/*.test.js` all green; `node --check` clean on every phase-touched file
</verification>

<success_criteria>
:root defines a spacing scale; inline style= counts in recipes.js and settings.js drop
substantially, with form sections and action rows rendered via shared utility classes.
(ROADMAP 07.1 success criteria #4 spacing-half and #5.)
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-recipes-and-ui-consistency-cleanup/07.1-05-SUMMARY.md`
</output>
