---
phase: 7.1
slug: recipes-and-ui-consistency-cleanup
status: verified
nyquist_compliant: partial
wave_0_complete: true
created: 2026-06-14
---

# Phase 7.1 — Validation Strategy

> Reconstructed from artifacts (State B) after execution. Phase 7.1 was planned in the
> streamlined path (no RESEARCH.md / VALIDATION.md up front), so this audit is retroactive.
> Phase 7.1 is predominantly a UI/CSS refactor; the one requirement with a clean unit-test
> surface (CHIP-03) received a new deterministic regression test, and the four view/CSS
> requirements are verified via the automated browser UAT (07.1-UAT.md) + grep gates rather
> than node:test units.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test (built-in), browser IIFE modules loaded via `vm.runInThisContext` |
| **Config file** | none — plain `node tests/*.test.js` |
| **Quick run command** | `node tests/phase-07_1-cleanup.test.js` |
| **Full suite command** | `for t in tests/*.test.js; do node "$t"; done` |
| **Estimated runtime** | ~2 seconds |
| **Browser UAT** | headless Chromium (`playwright-core`, `/opt/pw-browsers/chromium-1194`) against `python3 -m http.server 8000`; see 07.1-UAT.md |

---

## Sampling Rate

- **After every task commit:** `node --check` on modified JS + relevant `tests/*.test.js`
- **After every plan wave:** full suite (all 5 `tests/*.test.js`)
- **Before `/gsd-verify-work`:** full suite green — confirmed (5 files, 62+5 assertions, `# fail 0`)

---

## Per-Requirement Verification Map

| Requirement | Plan | Behavior | Test Type | Automated Command / Evidence | Status |
|-------------|------|----------|-----------|------------------------------|--------|
| CHIP-03 | 01 | Compat shim removed; `get('recipes')` returns raw pool, no synthesized v1 getters | unit (new) | `node tests/phase-07_1-cleanup.test.js` (5/5) + `grep -rnE "State\.get\('recipes'\)\?\.(originals\|confirmed_favorites\|wishlist\|made_log)" app/js/` → 0 | ✅ green |
| CHIP-04 | 02 | Originals chips carry ♥/☆/✓ + Ask-Bjorn via single RecipeChip path | manual (browser) | 07.1-UAT Test 2 — Originals chip screenshot shows the overlay action row + ORIGINAL badge | ✅ verified |
| RECIPE-GEN-01 | 03 | One Generate-with-AI pipeline; no "Create Recipe" CTA; all AI writes via WriteGate | manual + grep | `grep -c "Create Recipe" app/js/views/recipes.js` → 0; `grep -c handleGenerate` → 0; WriteGate at recipes.js:377/:693; 07.1-UAT Test 3 | ✅ verified |
| UI-TOKEN-01 | 04 | `--fs-*` type scale resolves; badge floor lifted to `--fs-xs` | manual (computed styles) | 07.1-UAT Test 4 — runtime `--fs-xs=0.75rem`; `.badge-status`=11.25px (--fs-xs), `.recipe-chip-name`=15.75px (--fs-lg) | ✅ verified |
| UI-TOKEN-02 | 05 | `--space-*` scale + named classes present and applied | manual (computed styles) | 07.1-UAT Test 5 — runtime `--space-*` resolve; all classes in stylesheet; `.muted-help`×4 in Settings | ✅ verified |

---

## Wave 0 Requirements

- [x] `tests/phase-07_1-cleanup.test.js` — CHIP-03 pool-contract regression (5 assertions): legacy→pool coercion, no synthesized v1 getters, stable get() reference, bucket→status/overlay mapping, v2 idempotency.
- [x] Existing infrastructure (phase-05/06/07 suites) covers the engine/normalize/AI behavior the refactor must preserve — all green post-refactor.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Originals chip overlay actions render | CHIP-04 | DOM/view wiring; no headless DOM in node:test harness | Load `#recipes` → Originals tab; a chip shows ♥/☆/○ + "Ask Bjorn about this" (verified in 07.1-UAT via injected-fixture render) |
| Unified Generate-with-AI flow | RECIPE-GEN-01 | Needs DOM + a live BYOK Anthropic key for the end-to-end draft | With a key: click "Generate with AI" → draft appears in Drafts via WriteGate; "Tweak with AI" refines. UI wiring auto-verified; live generation deferred (07.1-UAT Test 3/7). |
| Type + spacing tokens render correctly | UI-TOKEN-01/02 | CSS has no node:test surface | Computed-style assertions in headless Chromium (07.1-UAT Tests 4-5) confirm token resolution and the lifted badge floor. |
| Overlay toggle persistence to GitHub | CHIP-04 | Needs a real GitHub PAT + repo | With a configured PAT: toggle ♥ on a chip → State.save writes the recipe file (07.1-UAT Test 6, blocked in-container). |

---

## Validation Audit 2026-06-14

| Metric | Count |
|--------|-------|
| Requirements | 5 |
| Automated (unit) | 1 (CHIP-03) |
| Automated (browser UAT / computed-style / grep) | 4 |
| Gaps found | 1 (CHIP-03 lacked a dedicated unit test) |
| Gaps resolved | 1 (added `tests/phase-07_1-cleanup.test.js`) |
| Escalated / manual-only | 4 (UI/CSS/live-dependency) |

---

## Validation Sign-Off

- [x] CHIP-03 has a dedicated automated regression test; full suite green (5 files)
- [x] The 4 UI/CSS requirements have automated browser-UAT + grep verification (07.1-UAT.md)
- [x] Remaining unverifiable items (live GitHub write, live Anthropic generation) documented as prerequisite gates, not gaps
- [x] `nyquist_compliant: partial` — appropriate for a UI/CSS refactor phase; no code behavior is unverified
