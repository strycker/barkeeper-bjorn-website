---
phase: 07.1-recipes-and-ui-consistency-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/js/views/recommender.js
  - app/js/views/dashboard.js
  - app/js/views/profile.js
  - app/js/export.js
  - app/js/claude-api.js
  - app/js/state.js
autonomous: true
requirements: [CHIP-03]
must_haves:
  truths:
    - "No source file outside the recipes view relies on the legacy State.get('recipes') derived getters (.originals / .confirmed_favorites / .wishlist / .made_log)"
    - "The Recommender ♥/☆/✓ filled-state checks read the canonical pool, not the shim getters"
    - "The compat shim (_withRecipesShim / _resolveSeededForShim and the get('recipes') special-case) is deleted from state.js"
    - "node tests/*.test.js stays fully green after the migration"
  artifacts:
    - path: "app/js/state.js"
      provides: "get('recipes') returns the raw pool object with no synthetic getters"
      contains: "function get(key)"
    - path: "app/js/views/recommender.js"
      provides: "pool-based favorite/wishlist/made filled-state derivation"
  key_links:
    - from: "app/js/views/recommender.js"
      to: "State.get('recipes').pool"
      via: "filter on is_favorite / is_wishlist / made_log + status==='original'"
      pattern: "\\.pool"
    - from: "app/js/state.js"
      to: "_data.recipes"
      via: "get('recipes') returns _data.recipes directly"
      pattern: "return _data\\[key\\]"
---

<objective>
Finish the chip-unification migration (Commit 3 final step): remove the read-only
compat shim that synthesizes `.originals` / `.confirmed_favorites` / `.wishlist` /
`.made_log` arrays on `State.get('recipes')`, after migrating every remaining caller
to read the canonical `pool` array directly. This is CHIP-03.

Purpose: The shim was a transitional bridge. While it exists, two storage mental
models coexist (pool + synthetic v1 arrays), which makes Phase 8 (Portability) MD
export/import mapping ambiguous. Removing it leaves one source of truth.

Output: Migrated readers in recommender.js, dashboard.js, profile.js, export.js,
claude-api.js; the shim deleted from state.js; all tests green.

IMPORTANT REALITY NOTE (verified against source): the planning brief said the shim
lives in normalize.js. It does NOT. The shim is in **state.js** (`_withRecipesShim`,
`_resolveSeededForShim`, and the `if (key === 'recipes')` branch of `get`). normalize.js
has no derived-getters block. Plan accordingly. Also: recommender.js is the heaviest
shim consumer (9 getter reads), not just export.js/claude-api.js/profile.js as the
brief listed — dashboard.js reads `.originals` too. ALL of these must migrate before
the shim can be deleted, or filled-state badges break.

No formal UI-SPEC.md / RESEARCH.md / CONTEXT.md exists for this phase (streamlined run).
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/chip-unification-plan.md
@CLAUDE.md

<interfaces>
<!-- Canonical pool shape (from app/js/normalize.js recipe()/recipes()). Read these directly. -->
State.get('recipes') -> { _schema_version: 2, pool: Recipe[], last_updated: string }

Recipe (pool entry):
  id: string
  status: 'classic' | 'original' | 'draft'
  seed_id?: string            // present iff overlay-only reference to CLASSICS_DB
  name, base, method, ...     // core fields (absent on seeded entries — read via seed)
  is_favorite: boolean        // overlay flag
  is_wishlist: boolean        // overlay flag
  is_original: boolean        // overlay tag
  made_log: Array<{date, times_made?, notes?}>

<!-- The shim being deleted (app/js/state.js ~lines 245-284): -->
_withRecipesShim(rec)  -> defines getters originals/confirmed_favorites/wishlist/made_log
_resolveSeededForShim(entry) -> merges live CLASSICS_DB row over a seed_id pool entry
get('recipes') -> currently returns _withRecipesShim(_data.recipes)

<!-- Seeded-entry resolution helper to PORT into recommender.js (since the shim
     previously resolved seed core for sameRecipe comparisons). For filled-state
     checks the recommender already has the recipe object in hand (item.recipe has
     resolved core from the engine pool), so compare pool overlay flags by id/seed_id,
     NOT by re-resolving names. Use the _togglePoolFlag lookup logic already present
     in recommender.js (lines ~352-391) as the matching template. -->

<!-- RecommenderEngine.recommend opts.originals (app/js/recommender-engine.js line 250):
     expects an array of user-original recipe objects with base + ingredients.
     Replace `State.get('recipes')?.originals || []` with a pool filter:
     (State.get('recipes')?.pool || []).filter(r => r.status === 'original') -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate Recommender readers to the pool, drop all shim-getter reads</name>
  <read_first>
    - app/js/views/recommender.js (lines 60-145 _renderCard/_renderTwoAwayCard; lines 340-495 toggle handlers + _attach engine calls; lines 600-615 initial recommend call)
    - app/js/recommender-engine.js (lines 245-255 — opts.originals consumption)
    - app/js/state.js (lines 245-284 — the shim being removed; understand what the getters returned so the replacement filters match)
    - .planning/chip-unification-plan.md (Commit 3 final step + behavior matrix)
  </read_first>
  <action>
    In app/js/views/recommender.js, replace every legacy shim-getter read with a direct
    pool read. Specific edits (exact current strings → replacements):

    1. _renderCard (lines ~68-71) and _renderTwoAwayCard (lines ~128-131):
       Replace the three reads:
         const savedRecipes = State.get('recipes') || {};
         const isFav  = (savedRecipes.confirmed_favorites || []).some(r => Utils.sameRecipe(r, recipe));
         const isWish = (savedRecipes.wishlist || []).some(r => Utils.sameRecipe(r, recipe));
         const isMade = (savedRecipes.made_log || []).some(r => Utils.sameRecipe(r, recipe));
       with a single pool-based derivation. Add a module-level helper near _togglePoolFlag:
         function _poolEntryFor(probe) {
           const pool = (State.get('recipes')?.pool) || [];
           const classicsDb = (typeof CLASSICS_DB !== 'undefined' && Array.isArray(CLASSICS_DB)) ? CLASSICS_DB : [];
           let e = pool.find(p => p.id === probe.id);
           if (!e) e = pool.find(p => p.seed_id && p.seed_id === probe.id);
           if (!e) e = pool.find(p => p.status === 'original' && Utils.sameRecipe(p, probe));
           if (!e) e = pool.find(p => {
             if (!p.seed_id) return false;
             const seed = classicsDb.find(c => c.id === p.seed_id);
             return seed && Utils.sameRecipe(seed, probe);
           });
           return e || null;
         }
       Then in each card renderer:
         const _e = _poolEntryFor(recipe);
         const isFav  = !!(_e && _e.is_favorite);
         const isWish = !!(_e && _e.is_wishlist);
         const isMade = !!(_e && Array.isArray(_e.made_log) && _e.made_log.length > 0);
       (This mirrors the exact lookup ladder already in _togglePoolFlag — keep them consistent.)

    2. Toggle handlers (lines ~410, ~431, ~452): the pre-patch "was it on?" reads:
         const isFav = (State.get('recipes')?.confirmed_favorites || []).some(r => Utils.sameRecipe(r, probe));
         const isWish = (State.get('recipes')?.wishlist || []).some(r => Utils.sameRecipe(r, probe));
         const isMade = (State.get('recipes')?.made_log || []).some(r => Utils.sameRecipe(r, probe));
       Replace each with the _poolEntryFor(probe) form:
         const _e = _poolEntryFor(probe); const isFav  = !!(_e && _e.is_favorite);
         const _e = _poolEntryFor(probe); const isWish = !!(_e && _e.is_wishlist);
         const _e = _poolEntryFor(probe); const isMade = !!(_e && (_e.made_log || []).length > 0);

    3. Engine calls (lines ~478, ~490, ~520, ~558, ~610): replace every occurrence of
         originals: State.get('recipes')?.originals || []
       with
         originals: (State.get('recipes')?.pool || []).filter(r => r.status === 'original')
       (5 call sites — change ALL of them; grep to confirm zero remain.)

    Do NOT change _togglePoolFlag itself (already pool-aware). Do NOT change the
    rest of the recommender. Verify recommender-engine.js opts.originals still
    receives objects with base+ingredients — pool originals carry both, so no
    engine change is needed.
  </action>
  <verify>
    <automated>grep -nE "confirmed_favorites|\.wishlist|\.made_log|\?\.originals|\.originals \|\|" app/js/views/recommender.js | grep -v "is_favorite\|is_wishlist\|made_log:" ; test $(grep -cE "State\.get\('recipes'\)\?\.(originals|confirmed_favorites|wishlist|made_log)" app/js/views/recommender.js) -eq 0 && echo PASS-no-shim-reads</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "State.get('recipes')?.originals" app/js/views/recommender.js returns 0
    - grep -cE "confirmed_favorites|\.wishlist\b|\.made_log\b" app/js/views/recommender.js returns 0 (excluding the literal overlay-key strings inside _togglePoolFlag like `made_log: []`)
    - grep -c "_poolEntryFor" app/js/views/recommender.js is >= 1 (helper defined) and used in both card renderers + 3 toggle handlers
    - node --check app/js/views/recommender.js exits 0
  </acceptance_criteria>
  <done>Recommender derives ♥/☆/✓ filled state and engine originals from the pool; no shim getters referenced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate dashboard / profile / export / claude-api readers to the pool</name>
  <read_first>
    - app/js/views/dashboard.js (lines 1-16 — `recipes.originals` count)
    - app/js/views/profile.js (lines 334-342 — `State.get('recipes')?.originals` count)
    - app/js/export.js (lines 54-175 — exportAIContext uses recipes.originals / .confirmed_favorites / .wishlist)
    - app/js/claude-api.js (lines 280-378 — deriveContextMarkdown uses recipes.originals)
    - app/js/normalize.js (recipe()/recipes() — confirm pool entry field names)
  </read_first>
  <action>
    Replace the shim-getter reads in the remaining four callers with direct pool filters.
    Define the filters inline (the pool is small; no shared helper needed across files).

    1. app/js/views/dashboard.js line ~13:
       Replace: const originals = (recipes.originals || []).length;
       With:    const originals = ((recipes.pool || []).filter(r => r.status === 'original')).length;

    2. app/js/views/profile.js line ~339:
       Replace: const total = (State.get('recipes')?.originals || []).length;
       With:    const total = ((State.get('recipes')?.pool || []).filter(r => r.status === 'original')).length;

    3. app/js/export.js exportAIContext:
       - line ~128: const originals = recipes.originals || [];
         → const pool = recipes.pool || [];
           const originals = pool.filter(r => r.status === 'original');
       - line ~155: const favorites = recipes.confirmed_favorites || [];
         → const favorites = pool.filter(r => r.is_favorite);
       - line ~165: const wishlist = recipes.wishlist || [];
         → const wishlist = pool.filter(r => r.is_wishlist);
       NOTE: seeded (classic) favorites/wishlist entries are overlay-only (no name/core
       in the pool entry). To keep the export readable, resolve seed core for any entry
       with seed_id before emitting. Add a local resolver at the top of exportAIContext:
         const _classics = (typeof CLASSICS_DB !== 'undefined' && Array.isArray(CLASSICS_DB)) ? CLASSICS_DB : [];
         const _resolve = (e) => (e && e.seed_id) ? ({ ...(_classics.find(c => c.id === e.seed_id) || {}), ...e }) : e;
       and map favorites/wishlist/originals through _resolve before the forEach loops
       (originals never have seed_id so _resolve is a no-op for them, but mapping is harmless).

    4. app/js/claude-api.js deriveContextMarkdown line ~345:
       Replace: const originals = (recipes.originals || []).filter(r => r && r.name);
       With:    const originals = (recipes.pool || []).filter(r => r && r.status === 'original' && r.name);
       (The made-log block further down at ~379 also reads recipes.made_log — replace
        `(recipes.made_log || [])` with `(recipes.pool || []).filter(r => Array.isArray(r.made_log) && r.made_log.length)`
        and adjust the per-row read to use r.made_log[r.made_log.length-1] for date/notes;
        resolve seed core via the same _resolve pattern if seed_id present so the name prints.)
  </action>
  <verify>
    <automated>test $(grep -cE "\.(originals|confirmed_favorites|wishlist|made_log)\b" app/js/views/dashboard.js app/js/views/profile.js app/js/export.js app/js/claude-api.js | grep -vE "made_log:|is_favorite|is_wishlist" | awk -F: '{s+=$2} END{print s+0}') -eq 0 && echo CHECK; node --check app/js/views/dashboard.js && node --check app/js/views/profile.js && node --check app/js/export.js && node --check app/js/claude-api.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -rnE "State\.get\('recipes'\)\?\.(originals|confirmed_favorites|wishlist|made_log)" app/js/ returns ZERO matches across the whole app (recommender migrated in Task 1; these four migrated here)
    - grep -nE "recipes\.(originals|confirmed_favorites|wishlist)\b" app/js/export.js app/js/claude-api.js app/js/views/dashboard.js returns ZERO matches
    - node --check passes on all four files
  </acceptance_criteria>
  <done>Dashboard count, profile count, AI-context export, and Claude context builder all read the canonical pool; no shim getters remain anywhere outside state.js.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete the compat shim from state.js and verify the full suite</name>
  <read_first>
    - app/js/state.js (lines 245-307 — _resolveSeededForShim, _withRecipesShim, get())
    - tests/phase-07-ai.test.js (load order + how it stubs CLASSICS_DB)
    - .planning/chip-unification-plan.md (acceptance/verification section)
  </read_first>
  <action>
    Pre-check FIRST: run `grep -rn "State.get('recipes')" app/js/` and confirm every
    remaining caller reads `.pool` (recipes.js view + the migrated callers). If any
    caller still reads a v1 getter (.originals/.confirmed_favorites/.wishlist/.made_log),
    STOP and migrate it before deleting the shim — deleting with a live consumer would
    silently return undefined and break a badge/count.

    Then in app/js/state.js:
    1. Delete the _resolveSeededForShim function (lines ~259-269).
    2. Delete the _withRecipesShim function (lines ~271-279).
    3. Delete the surrounding comment block (lines ~245-258) describing the shim.
    4. Change get() (lines ~281-284) from:
         function get(key) {
           if (key === 'recipes') return _withRecipesShim(_data.recipes);
           return _data[key];
         }
       to:
         function get(key) {
           return _data[key];
         }
    Leave set/patch/save and the rest of state.js untouched.

    After editing, run the full test suite and node --check on every JS file touched
    in this plan. If any test fails, the migration missed a caller — diagnose via the
    failing assertion and fix the caller (do NOT restore the shim).
  </action>
  <verify>
    <automated>test $(grep -c "_withRecipesShim\|_resolveSeededForShim" app/js/state.js) -eq 0 && echo SHIM-GONE && for t in tests/*.test.js; do node "$t" || exit 1; done && echo TESTS-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "_withRecipesShim" app/js/state.js returns 0
    - grep -c "_resolveSeededForShim" app/js/state.js returns 0
    - grep -A2 "function get(key)" app/js/state.js shows it returns `_data[key]` with no recipes special-case
    - `node tests/phase-05-engine.test.js && node tests/phase-05-normalize.test.js && node tests/phase-06-engine.test.js && node tests/phase-07-ai.test.js` all exit 0 (# fail 0)
    - grep -rnE "State\.get\('recipes'\)\?\.(originals|confirmed_favorites|wishlist|made_log)" app/js/ returns ZERO across the codebase
  </acceptance_criteria>
  <done>The compat shim is gone from state.js; get('recipes') returns the raw pool object; the full deterministic test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage/GitHub JSON → State pool | Persisted recipe data deserialized into the in-memory pool (already normalized on load by Normalize.recipes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-071-01 | Tampering | state.js get('recipes') | accept | Pure refactor: removing synthetic getters does not change what data is read or written; Normalize.recipes still coerces on load/set (state.js set() calls _normalize). No new input surface. |
| T-071-02 | Information Disclosure | export.js / claude-api.js seed-core resolution | accept | Resolving seed_id → CLASSICS_DB only exposes already-public classics data into the user's own export/context; no cross-user or secret data. Existing behavior (shim did the same resolution). |
| T-071-03 | Denial of Service | recommender.js _poolEntryFor over the pool | accept | Pool is user-scale (tens to low hundreds of entries); linear find per card is equivalent to the prior shim filter cost. No regression. |

No NEW threat introduced by this consolidation — it removes code paths rather than adding input surfaces.
</threat_model>

<verification>
- `grep -rnE "State\.get\('recipes'\)\?\.(originals|confirmed_favorites|wishlist|made_log)" app/js/` → zero matches
- `grep -c "_withRecipesShim\|_resolveSeededForShim" app/js/state.js` → 0
- `node tests/*.test.js` → all green (# fail 0)
- `node --check` passes on all six modified files
</verification>

<success_criteria>
No source file calls the legacy State.get('recipes') derived getters; the shim
is deleted from state.js (note: brief said normalize.js — corrected to actual
location); the full deterministic test suite is green. (ROADMAP 07.1 success criterion #1.)
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-recipes-and-ui-consistency-cleanup/07.1-01-SUMMARY.md`
</output>
