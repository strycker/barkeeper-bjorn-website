---
status: complete
phase: 07.1-recipes-and-ui-consistency-cleanup
source: [07.1-01-SUMMARY.md, 07.1-02-SUMMARY.md, 07.1-03-SUMMARY.md, 07.1-04-SUMMARY.md, 07.1-05-SUMMARY.md]
started: 2026-06-14
updated: 2026-06-14
method: automated (headless Chromium via playwright-core + app served on :8000; fixtures = repo data/*.json injected via State.set, views rendered directly to bypass the GitHub auth gate)
---

## Current Test

[testing complete]

## Tests

### 1. App boots + all views render (CHIP-03 regression guard)
expected: With the compat shim deleted, the app loads and every view still reads the canonical pool without a runtime error.
result: pass
evidence: 5 views (Recipes, Settings, Classroom, Library, Chat) rendered — `page_errors: []` (zero JS exceptions). `State.get('recipes').pool` = 14 entries read cleanly. The only console error is `ERR_TUNNEL_CONNECTION_FAILED` (the Bjorn avatar image from raw.githubusercontent, blocked by the sandbox proxy — not a code error).

### 2. Originals chip parity — ♥/☆/✓ + Ask-Bjorn (CHIP-04)
expected: Originals chips render through the single RecipeChip path with the favorite/wishlist/made overlay actions and "Ask Bjorn about this", matching the other tabs.
result: pass
evidence: Recipes → Originals tab screenshot — "Smokey the Pear" chip shows the ORIGINAL status badge, the ♥ ☆ ○ overlay action buttons, ✕ remove, "Edit", "Ask Bjorn about this", and "Tweak with AI". Identical action set to Favorites/Wishlist/Made chips.

### 3. Unified "Generate with AI" entry point + CTA rename (RECIPE-GEN-01)
expected: One AI-generation surface; the ambiguous "Create Recipe" CTA is gone.
result: pass (UI verified) — live generation deferred
evidence: Recipe Book header shows "✨ Generate with AI"; chip shows "Tweak with AI"; `grep -c "Create Recipe"` = 0. The generation pipeline routes through WriteGate (code-verified: recipes.js:377/:693; `handleGenerate` removed). NOTE: actually producing a draft from Claude needs a live BYOK Anthropic key — deferred to a keyed session (same class as Phase 7's live-key UAT).

### 4. Type scale resolves at runtime; badge floor lifted (UI-TOKEN-01)
expected: `--fs-xs..--fs-2xl` exist in :root and the migrated selectors resolve to them; the status badge is no longer the sub-floor 0.68rem.
result: pass
evidence: computed `:root` → `--fs-xs:0.75rem`, `--fs-base:0.95rem`, `--fs-2xl:1.5rem`. Live computed font-size: `.badge-status` = 11.25px (= --fs-xs, the lifted floor), `.recipe-chip-name` = 15.75px (= --fs-lg). Badge is now the floor token, not below it.

### 5. Spacing tokens + named classes present and applied (UI-TOKEN-02)
expected: `--space-*` in :root; the named form/utility classes exist in the stylesheet and are used.
result: pass
evidence: computed `:root` → `--space-1:4px`, `--space-3:12px`, `--space-6:24px`. All classes found in the loaded stylesheet: form-section-card, form-actions-row, form-check-row, muted-help, input-disabled, empty-hint, form-status, section-desc, mt-3, mb-2. Settings view renders 4 `.muted-help` elements + section-card framing (screenshot).

### 6. Live persistence of overlay toggles to GitHub
expected: Clicking ♥/☆/✓ on a chip flips the overlay flag and persists via State.save → GitHub Contents API.
result: blocked
blocked_by: third-party
reason: "Requires a real GitHub PAT + repo; the headless run uses a dummy token and never calls the live API. In-memory State.set/patch path is exercised (chip renders correct filled state from the pool); the network write is unverified here."

### 7. Live AI generation quality (draft content)
expected: "Generate with AI" / "Tweak with AI" produce a sensible balanced draft via Claude.
result: blocked
blocked_by: third-party
reason: "Requires a live BYOK Anthropic key. Pipeline wiring + WriteGate routing are code-verified; output quality needs a keyed session."

## Summary

total: 7
passed: 5
issues: 0
blocked: 2
pending: 0
skipped: 0

## Gaps

[none — all failures are prerequisite gates (live GitHub / live Anthropic), not code defects]

## Notes

Automated visual UAT run in headless Chromium. Screenshots captured: recipes (Originals
chip with overlay actions), settings (section cards + muted-help), classroom, library, chat —
all rendered without JS exceptions. The two blocked items mirror Phase 7's deferred live-key
UAT and require the maintainer's own authenticated session; they are not code defects and do
not block phase completion.
