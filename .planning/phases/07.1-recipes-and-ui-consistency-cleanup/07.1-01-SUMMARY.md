---
phase: 07.1-recipes-and-ui-consistency-cleanup
plan: "01"
subsystem: data-layer
tags: [chip-unification, recipes, pool, shim-removal, refactor]
dependency_graph:
  requires: []
  provides: [canonical-pool-reader, shim-free-state]
  affects: [recommender, dashboard, profile, export, claude-api, state]
tech_stack:
  added: []
  patterns: [pool-filter, seed-resolver, inline-iife]
key_files:
  created: []
  modified:
    - app/js/state.js
    - app/js/views/recommender.js
    - app/js/views/dashboard.js
    - app/js/views/profile.js
    - app/js/export.js
    - app/js/claude-api.js
decisions:
  - "Delete shim inline with migration — no intermediate half-migrated state"
  - "Seed resolution helper defined locally per-file (export.js _resolveExport, claude-api.js _resolveCtx) rather than shared module, consistent with vanilla no-bundler architecture"
  - "claude-api.js made-log: use lastEntry.times_made || made_log.length as frequency signal (pool made_log is an array of {date, times_made?, notes?} per entry)"
metrics:
  duration: "~4 minutes"
  completed: "2026-06-14"
  tasks_completed: 3
  tasks_total: 3
  files_changed: 6
---

# Phase 07.1 Plan 01: CHIP-03 Shim Removal Summary

**One-liner:** Removed `_withRecipesShim`/`_resolveSeededForShim` from state.js after migrating all 5 remaining callers to read the canonical pool array directly, leaving `State.get('recipes')` returning raw `_data.recipes` with no synthetic getters.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Migrate recommender.js to pool-based filled-state reads | 35f8faf | app/js/views/recommender.js |
| 2 | Migrate dashboard/profile/export/claude-api to pool reads | 74f4f48 | app/js/views/dashboard.js, app/js/views/profile.js, app/js/export.js, app/js/claude-api.js |
| 3 | Delete compat shim from state.js | ffbe174 | app/js/state.js |

## What Was Done

**Task 1 (pre-committed):** `recommender.js` was already migrated before this execution session. It defines `_poolEntryFor(probe)` using the 4-step lookup ladder (by id, seed_id, original sameRecipe, seeded sameRecipe) and uses it in `_renderCard`, `_renderTwoAwayCard`, and the 3 toggle click handlers. All 5 engine `originals:` call sites were already updated to `(State.get('recipes')?.pool || []).filter(r => r.status === 'original')`.

**Task 2:** Four remaining files migrated:
- `dashboard.js` — 3 shim reads replaced: `recipes.originals` count (line 13), `recipes.confirmed_favorites` stat count (line 89), `recipes.originals` recent-grid slice (line 185)
- `profile.js` — `State.get('recipes')?.originals` count replaced with pool filter
- `export.js` — Added `_resolveExport` helper (merges CLASSICS_DB seed core over overlay entry for name resolution); replaced `recipes.originals`, `recipes.confirmed_favorites`, `recipes.wishlist` with `pool.filter()` variants; `pool` variable extracted before the filter calls
- `claude-api.js` — Replaced `recipes.originals` with pool filter; replaced `recipes.made_log` shim read with a pool iteration that finds entries with `made_log.length > 0`, resolves seed name via `_resolveCtx`, and reads `lastEntry.date` / `lastEntry.times_made` from the per-entry made_log array

**Task 3:** Deleted from state.js:
- The 14-line comment block describing the shim's purpose
- `_resolveSeededForShim()` (11 lines)
- `_withRecipesShim()` (9 lines)
- The `if (key === 'recipes') return _withRecipesShim(...)` branch in `get()`

`get()` is now a clean 3-line function returning `_data[key]` unconditionally.

## Verification Results

```
grep -rnE "State\.get\('recipes'\)\?\.(originals|confirmed_favorites|wishlist|made_log)" app/js/
→ ZERO matches

grep -c "_withRecipesShim" app/js/state.js  → 0
grep -c "_resolveSeededForShim" app/js/state.js  → 0

node tests/phase-05-engine.test.js    → # fail 0  (1 test)
node tests/phase-05-normalize.test.js → # fail 0  (9 tests)
node tests/phase-06-engine.test.js    → # fail 0  (9 tests)
node tests/phase-07-ai.test.js        → # fail 0  (43 tests)

node --check on all 6 modified files  → syntax OK
```

## Deviations from Plan

None. The plan was executed exactly as written, with one note: `recommender.js` (Task 1) was already committed before this execution session began (commit `35f8faf`). All other tasks ran sequentially during this session.

## Known Stubs

None. All pool reads are wired to live `State.get('recipes').pool` data. Seed resolution in export.js and claude-api.js uses `CLASSICS_DB` (the read-only seed file loaded at runtime).

## Threat Flags

None. This plan removes code paths (the shim) rather than adding any new input surface. No new endpoints, auth paths, or schema changes at trust boundaries.

## Self-Check: PASSED

- [x] app/js/state.js exists and contains `function get(key) { return _data[key]; }`
- [x] Commit 35f8faf exists (Task 1)
- [x] Commit 74f4f48 exists (Task 2)
- [x] Commit ffbe174 exists (Task 3)
- [x] grep returns 0 matches for shim getter patterns
- [x] All 4 test files report `# fail 0`
- [x] STATE.md and ROADMAP.md not modified
