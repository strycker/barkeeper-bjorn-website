---
phase: 07.1-recipes-and-ui-consistency-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/css/app.css
autonomous: true
requirements: [UI-TOKEN-01]
must_haves:
  truths:
    - ":root defines a six-step type scale --fs-xs through --fs-2xl"
    - "The chip / lesson-tile / status-badge / chat-bubble selectors reference var(--fs-*) instead of raw rem values"
    - "The smallest font in the migrated selectors is no smaller than --fs-xs (0.75rem) — the 0.68rem badge floor is lifted"
  artifacts:
    - path: "app/css/app.css"
      provides: "Type-scale token definitions in :root + migrated Phase 7 selectors"
      contains: "--fs-xs"
  key_links:
    - from: ".recipe-chip-name / .badge-status / .chat-bubble / .lesson-tile-* selectors"
      to: ":root --fs-* tokens"
      via: "font-size: var(--fs-...)"
      pattern: "font-size: var\\(--fs-"
---

<objective>
Add a six-step type scale to :root and migrate the worst ad-hoc font-size sprawl onto it
(UI-TOKEN-01). The Phase 7 UI Review scored Typography 2/4: 30+ distinct font-size values,
no scale, and a 0.68rem status badge that sits below the accessibility floor.

This plan is CSS-only and fully independent of the recipes.js / state.js work — it runs in
Wave 1 in parallel with CHIP-03.

Purpose: Make vertical rhythm auditable (grep one token set) and lift the accessibility
floor off 0.68rem.

Output: --fs-xs … --fs-2xl in :root; the chip system, lesson tiles, status badges, chat
bubbles, classroom, and library text selectors snapped onto the scale.

DESIGN CONTRACT: No formal UI-SPEC.md exists for this phase. Per the planning brief, the
Phase 7 UI Review's "Top 3 Priority Fixes" (07-UI-REVIEW.md, fix #1) IS the design contract.
The exact selectors and current values were extracted from app.css and are listed per-line
in the action below.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ai-integration/07-UI-REVIEW.md
@CLAUDE.md

<interfaces>
<!-- Existing :root tokens (app/css/app.css lines 4-24): colors + --radius/--radius-sm/
     --shadow/--transition. NO --fs-* or --space-* yet. Add the type scale immediately
     after --radius-sm (line ~21), before --shadow. -->

<!-- Six-step scale to define (chosen to map cleanly onto existing values and lift the
     0.68rem floor to a 0.75rem accessibility floor): -->
  --fs-xs:   0.75rem;   /* small meta / badges / tags / urls — floor */
  --fs-sm:   0.85rem;   /* taglines, secondary body, error bubble, labels */
  --fs-base: 0.95rem;   /* default body-adjacent, chat bubble, inputs, lesson body */
  --fs-lg:   1.05rem;   /* chip name, library form title, card titles */
  --fs-xl:   1.2rem;    /* section/topic headings */
  --fs-2xl:  1.5rem;    /* page H1 (chat header) */
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define the six-step type scale in :root</name>
  <read_first>
    - app/css/app.css (lines 4-33 — :root block + html/body base font-size)
    - .planning/phases/07-ai-integration/07-UI-REVIEW.md (Pillar 4 + Top fix #1)
  </read_first>
  <action>
    In app/css/app.css, inside the :root block, immediately after the line
    `  --radius-sm: 5px;` (line ~21) and before `  --shadow:` , insert the type scale:

      /* Type scale (UI-TOKEN-01) — six steps; --fs-xs is the accessibility floor. */
      --fs-xs:   0.75rem;
      --fs-sm:   0.85rem;
      --fs-base: 0.95rem;
      --fs-lg:   1.05rem;
      --fs-xl:   1.2rem;
      --fs-2xl:  1.5rem;

    Do not change html/body base font-size (15px) or any other token.
  </action>
  <verify>
    <automated>grep -E '^\s*--fs-(xs|sm|base|lg|xl|2xl):' app/css/app.css | grep -c -- '--fs-' | grep -qx 6 && echo SIX-STEPS</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE '^\s*--fs-(xs|sm|base|lg|xl|2xl):' app/css/app.css returns 6
    - The tokens appear inside :root (before the closing brace at line ~24)
  </acceptance_criteria>
  <done>Six type-scale tokens defined in :root.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate chip / badge / chat / classroom / library selectors onto the scale</name>
  <read_first>
    - app/css/app.css (lines 1500-1875 — the Phase 7 selector block, all font-size values listed below)
  </read_first>
  <action>
    Replace the raw font-size values on the following selectors with the nearest scale token.
    Exact current line → token mapping (snap to nearest step; 0.68→xs lifts the floor):

      L1514 .chat-page .chat-header h1 : 1.4rem   → var(--fs-2xl)
      L1533 .chat-bubble              : 0.95rem  → var(--fs-base)
      L1551 .chat-bubble--error       : 0.85rem  → var(--fs-sm)
      L1562 .chat-composer textarea   : 0.95rem  → var(--fs-base)
      L1591 .classroom-sub            : 0.92rem  → var(--fs-base)
      L1595 .classroom-topic h2       : 1.15rem  → var(--fs-xl)
      L1621 .lesson-tile-title        : 1rem     → var(--fs-lg)
      L1626 .lesson-tile-body         : 0.9rem   → var(--fs-base)
      L1644 .library-sub              : 0.92rem  → var(--fs-base)
      L1658 .library-form (title/label area, 1.05rem) → var(--fs-lg)
      L1665 .library-form label       : 0.86rem  → var(--fs-sm)
      L1675 .library-form input/textarea : 0.92rem → var(--fs-base)
      L1709 .library-card-title       : 1rem     → var(--fs-lg)
      L1722 .library-card-url         : 0.78rem  → var(--fs-xs)
      L1729 .library-card-desc        : 0.9rem   → var(--fs-base)
      L1748 .library-tag              : 0.74rem  → var(--fs-xs)
      L1770 (library, 0.78rem)        : 0.78rem  → var(--fs-xs)
      L1813 .recipe-chip-name         : 1.05rem  → var(--fs-lg)
      L1819 .recipe-chip-flag         : 0.95rem  → var(--fs-base)
      L1823 .recipe-chip-meta         : 0.8rem   → var(--fs-sm)
      L1832 .recipe-chip-tagline      : 0.85rem  → var(--fs-sm)
      L1849 .badge-status / .badge    : 0.68rem  → var(--fs-xs)   (floor lift — key fix)

    Read each line first to confirm the selector before editing (line numbers may drift by
    a couple if Task 1 shifts them — match on the value + selector, not the absolute line).
    Change ONLY the font-size declaration on these selectors; leave color/padding/etc intact.
    Do NOT touch the pre-Phase-7 px-based font sizes (lines ~1062-1409) — those are out of
    scope for this token pass (the ROADMAP scope is the chip/lesson/badge/chat surfaces).
  </action>
  <verify>
    <automated>test $(grep -c 'font-size: var(--fs-' app/css/app.css) -ge 20 && echo MIGRATED && test $(awk -F: '$1>=1500' <(grep -nE 'font-size: ?0\.68rem' app/css/app.css) | wc -l) -eq 0 && echo FLOOR-LIFTED</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'font-size: var(--fs-' app/css/app.css is >= 20 (the migrated selectors)
    - grep -nE 'font-size: ?0\.68rem' app/css/app.css returns 0 (the badge floor is gone)
    - The migrated selectors (.recipe-chip-name, .recipe-chip-meta, .badge-status, .lesson-tile-title, .library-form label, .chat-bubble) all reference var(--fs-*) — verify each with grep -A1
    - No raw `font-size: 1.05rem` / `0.8rem` / `0.85rem` / `0.95rem` / `0.92rem` / `0.9rem` / `1.15rem` / `1.4rem` / `0.78rem` / `0.74rem` remain on the listed chip/chat/classroom/library/badge selectors. Concrete spot-greps (each must return 1, i.e. the selector references a token):
        - grep -A2 '\.badge-status' app/css/app.css | grep -c 'font-size: var(--fs-'  → 1
        - grep -A2 '\.recipe-chip-name' app/css/app.css | grep -c 'font-size: var(--fs-'  → 1
        - grep -A2 '\.recipe-chip-meta' app/css/app.css | grep -c 'font-size: var(--fs-'  → 1
        - grep -A2 '\.chat-bubble' app/css/app.css | grep -c 'font-size: var(--fs-'  → 1
        - grep -A2 '\.lesson-tile-title' app/css/app.css | grep -c 'font-size: var(--fs-'  → 1
  </acceptance_criteria>
  <done>Chip, badge, chat, classroom, and library text selectors reference the type scale; the 0.68rem floor is lifted to --fs-xs (0.75rem).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A six-step type scale (--fs-xs … --fs-2xl) now lives in :root, and the recipe-chip system, status badges, chat bubbles, classroom, and library text selectors reference it instead of raw rem values. The smallest font (status badge) lifted from 0.68rem to 0.75rem.</what-built>
  <how-to-verify>
    1. Serve the app (`python3 -m http.server 8000`, open http://localhost:8000/app/).
    2. Recipes tab: confirm chip names, taglines, meta lines, and status badges look correctly proportioned (no obviously oversized/undersized text vs before).
    3. Status badges (classic/original/draft pills) should be slightly larger and more readable than before.
    4. Open #classroom and #library and #chat: confirm headings, body text, labels, tags, and chat bubbles render at sensible sizes.
    5. Nothing should look broken or wildly resized — this is a snap-to-nearest-step migration.
  </how-to-verify>
  <resume-signal>Type "approved" or note any selector whose size looks wrong after the migration.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | CSS-only change; no data, input, or execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-074-01 | (n/a) | app.css token definitions | accept | Pure stylesheet refactor — adds CSS custom properties and swaps font-size values. No script, no input parsing, no data flow. No security surface. |

No NEW threat introduced — CSS token migration only. Stated explicitly per the phase security gate.
</threat_model>

<verification>
- `grep -cE '^\s*--fs-(xs|sm|base|lg|xl|2xl):' app/css/app.css` → 6
- `grep -c 'font-size: var(--fs-' app/css/app.css` → >= 20
- `grep -nE 'font-size: ?0\.68rem' app/css/app.css` → 0
- Human-verify checkpoint confirms nothing renders broken
</verification>

<success_criteria>
:root defines a six-step type scale; chip/lesson/badge/chat selectors reference the scale
variables instead of raw rem. (ROADMAP 07.1 success criterion #4, type-scale half.)
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-recipes-and-ui-consistency-cleanup/07.1-04-SUMMARY.md`
</output>
