---
phase: 06-recipe-recommender-ux
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - .planning/phases/06-recipe-recommender-ux/06-TEST-CHECKLIST.md
  - .planning/phases/06-recipe-recommender-ux/06-UAT.md
autonomous: true
requirements:
  - REC-10
  - REC-11
  - RECIPE-MADE-01
  - RECIPE-MADE-02
  - RECIPE-VIEW-01
  - RECIPE-VIEW-02
  - RECIPE-SEARCH-01
  - REC-SEARCH-01

must_haves:
  truths:
    - "06-TEST-CHECKLIST.md lists every Phase 6 behavior as a manual checkbox grouped by source (8 ROADMAP reqs + 3 gap tasks + D-06/D-07/D-08)"
    - "06-UAT.md follows the Phase 5 05-UAT.md format (frontmatter, numbered tests with expected/result, summary, gaps)"
    - "Every D-06/D-07/D-08 behavior in the RESEARCH Validation Architecture test maps to at least one UAT item"
    - "Each ROADMAP requirement is marked DONE with file:line evidence"
  artifacts:
    - path: ".planning/phases/06-recipe-recommender-ux/06-TEST-CHECKLIST.md"
      provides: "manual UAT checklist grouped by source artifact"
      contains: "D-06"
    - path: ".planning/phases/06-recipe-recommender-ux/06-UAT.md"
      provides: "Phase 5-format UAT record with all Phase 6 behaviors"
      contains: "## Tests"
  key_links:
    - from: "06-UAT.md tests"
      to: "RESEARCH Validation Architecture test maps"
      via: "one UAT item per mapped behavior"
      pattern: "result:"
---

<objective>
Implement D-09: produce the Phase 6 completion deliverables — `06-TEST-CHECKLIST.md` and
`06-UAT.md` — in the Phase 5 format. UAT scope per CONTEXT D-09 covers ALL of: the 8 ROADMAP
requirements, the 3 prior gap tasks (Originals-tab search, schema, modal tally — DONE), and the
new D-06 (editable modal), D-07 (Originals in Recommender, Strategy B), and D-08 (name+base dedup)
behaviors.

This is a documentation/checklist deliverable — no application code changes. There is NO automated
test framework in this project (vanilla JS SPA); all validation is manual browser UAT, plus the
engine `node --check`/inline-node smoke checks the code plans already define. The UAT is initiated
by the user via GSD commands — no auto-advance.

Purpose: a single source of truth for verifying Phase 6 before `/gsd-verify-work`.

Output: two markdown files in the phase directory.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-recipe-recommender-ux/06-CONTEXT.md
@.planning/phases/06-recipe-recommender-ux/06-RESEARCH.md
@.planning/phases/05-polish-depth-ux-tidy/05-UAT.md
@.planning/phases/05-polish-depth-ux-tidy/05-VALIDATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write 06-TEST-CHECKLIST.md (manual checklist grouped by source)</name>
  <read_first>.planning/phases/06-recipe-recommender-ux/06-RESEARCH.md (Validation Architecture sections — original + addendum); 05-VALIDATION.md (format)</read_first>
  <files>.planning/phases/06-recipe-recommender-ux/06-TEST-CHECKLIST.md</files>
  <action>
    Create the checklist with frontmatter (phase: 06, slug, status: pending, created date) then
    these sections, each item a `- [ ]` checkbox with the behavior and its source ID:

    A. ROADMAP requirements (8) — marked DONE with file:line evidence (these ship already):
       REC-10 (recommender.js:84-88), REC-11 (recommender.js:84-88/307-355),
       RECIPE-MADE-01 (recommender.js:87/357-381), RECIPE-MADE-02 (recipes.js:289-349),
       RECIPE-VIEW-01 (recipes.js:231-287), RECIPE-VIEW-02 (recipes.js:271-273/351-473),
       RECIPE-SEARCH-01 (recipes.js:49-63), REC-SEARCH-01 (recommender.js:487-493).
    B. Prior gap tasks (3) — DONE: Originals-tab search (recipes.js:179),
       schema made_log/savedRecipe (schema/recipes.schema.json:39-43,134-174),
       modal live tally refresh (recipes.js:437-447).
    C. D-06 editable modal — pull the 4 D-06 rows from RESEARCH "New-Decision Requirements → Test Map":
       editable inputs for Originals; read-only for classics-db; dual-write data inspection; rename safety.
    D. D-07 Originals in Recommender (Strategy B) — pull the 4 D-07 rows: appear with amber badge;
       excluded when no base+ingredients; no console crash; favorite stores _source:'originals'.
       Add one Strategy-B-specific item: an Original whose ingredients match inventory appears as
       buildable; one whose ingredients don't normalize may not (accepted tradeoff).
    E. D-08 dedup — pull the 5 D-08 rows: filled state by name+base; same-name/different-base distinct;
       toggle add/remove never duplicates; same recipe in Favorites AND Wishlist; made-again increments.
    F. Known cosmetic notes (accepted): Originals show "Advanced" difficulty chip (Pitfall F);
       string profile → neutral 0.5 mood ranking (Pitfall B).
    Include the quick-run code checks from the code plans (node --check on the 4 JS files;
    the inline engine smoke test from 06-03 Task 1) as an automated pre-UAT gate subsection.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/06-recipe-recommender-ux/06-TEST-CHECKLIST.md','utf8');for(const t of ['D-06','D-07','D-08','REC-10','RECIPE-MADE-01','Strategy B','- [ ]']){if(!s.includes(t))throw new Error('missing '+t)}if((s.match(/- \[ \]/g)||[]).length<18)throw new Error('too few checklist items');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\- \[ \]' .planning/phases/06-recipe-recommender-ux/06-TEST-CHECKLIST.md` returns >= 18
    - file contains all of: REC-10, RECIPE-MADE-01, D-06, D-07, D-08, "Strategy B"
    - file references file:line evidence for the 8 ROADMAP reqs (grep `recommender.js:` returns >= 4)
  </acceptance_criteria>
  <done>Checklist covers 8 ROADMAP reqs (DONE + evidence) + 3 gap tasks + D-06/D-07/D-08, grouped by source, with a pre-UAT automated-check subsection.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write 06-UAT.md (Phase 5 format)</name>
  <read_first>.planning/phases/05-polish-depth-ux-tidy/05-UAT.md (exact format: frontmatter, ## Current Test, ## Tests with ### N + expected/result, ## Summary, ## Gaps)</read_first>
  <files>.planning/phases/06-recipe-recommender-ux/06-UAT.md</files>
  <action>
    Mirror 05-UAT.md structure exactly:
    - Frontmatter: status: pending, phase: 06-recipe-recommender-ux, source list (the four 06-0N-SUMMARY.md),
      started/updated dates.
    - `## Current Test` → "[awaiting user]".
    - `## Tests` with numbered `### N. <Title>` entries, each with `expected:` (concrete browser steps,
      Phase 5 prose style) and `result:` (leave as `pending`). Cover, at minimum, one test per behavior
      from the RESEARCH Validation Architecture maps (original 10-row table + the 14-row addendum table),
      consolidated and de-duplicated. Each expected: must be executable by a person at
      `python3 -m http.server 8000` with no ambiguity (name the tab, the button, the field, the JSON file
      to inspect for data-inspection tests).
    - `## Summary` with total/passed/issues/pending/skipped (total = number of tests; pending = total).
    - `## Gaps` → "[none yet]".
    Do NOT mark anything pass — the user runs the UAT.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/06-recipe-recommender-ux/06-UAT.md','utf8');for(const t of ['## Tests','## Summary','## Gaps','expected:','result:']){if(!s.includes(t))throw new Error('missing '+t)}const tests=(s.match(/^### \d+\./gm)||[]).length;if(tests<15)throw new Error('expected >=15 tests, got '+tests);if(/result: pass/.test(s))throw new Error('UAT must not be pre-marked pass');console.log('OK '+tests)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^### [0-9]+\.' .planning/phases/06-recipe-recommender-ux/06-UAT.md` returns >= 15
    - file contains `## Tests`, `## Summary`, `## Gaps`
    - `grep -c 'result: pass' .planning/phases/06-recipe-recommender-ux/06-UAT.md` returns 0 (not pre-marked)
    - `grep -c 'result: pending' .planning/phases/06-recipe-recommender-ux/06-UAT.md` returns >= 15
  </acceptance_criteria>
  <done>06-UAT.md matches the Phase 5 format, has >=15 unambiguous tests covering all Phase 6 behaviors, none pre-marked pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan. No code, no input handling, no DOM, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | n/a | markdown deliverables | accept | No executable code or user input; no security-relevant surface introduced. |
</threat_model>

<verification>
- Both files exist in the phase directory.
- 06-TEST-CHECKLIST.md groups items by source and references file:line evidence for shipped reqs.
- 06-UAT.md is structurally identical to 05-UAT.md and covers D-06/D-07/D-08 plus the 8 reqs + 3 gap tasks.
- No UAT item is pre-marked pass.
</verification>

<success_criteria>
1. 06-TEST-CHECKLIST.md exists with >=18 checkboxes covering all four sources, with evidence for DONE items.
2. 06-UAT.md exists in Phase 5 format with >=15 unambiguous tests, none pre-marked pass.
3. Every D-06/D-07/D-08 behavior from RESEARCH Validation Architecture maps to a UAT item.
</success_criteria>

<output>
After completion, create `.planning/phases/06-recipe-recommender-ux/06-04-SUMMARY.md`
Suggested commit: `docs(06): add phase-6 test checklist and UAT record`
</output>
