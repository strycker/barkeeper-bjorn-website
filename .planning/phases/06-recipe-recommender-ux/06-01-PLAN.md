---
phase: 06-recipe-recommender-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/js/utils.js
  - app/js/views/recommender.js
  - app/js/views/recipes.js
autonomous: true
requirements:
  - REC-10
  - REC-11
  - RECIPE-MADE-01
  - RECIPE-MADE-02
  - RECIPE-VIEW-01
  - RECIPE-VIEW-02
  - RECIPE-SEARCH-01
  - REC-SEARCH-01

must_haves:
  truths:
    - "Two saved recipes that share a name but differ in base spirit are treated as distinct entries everywhere"
    - "Clicking a filled ♥/★/✓ button removes that exact recipe (name+base match); clicking an open button adds it; no duplicates are ever created"
    - "The same recipe can exist in Favorites and Wishlist simultaneously"
    - "Re-adding an already-made recipe via the modal increments times_made instead of creating a duplicate row"
    - "All recipe equality checks in the changed handlers route through the single Utils.sameRecipe helper (no inline name-only comparisons remain)"
  artifacts:
    - path: "app/js/utils.js"
      provides: "Utils.sameRecipe(a, b) — case-insensitive name AND base comparator"
      contains: "sameRecipe"
    - path: "app/js/views/recommender.js"
      provides: "name+base dedup at all read checks + toggle handlers; data-base on the 3 action buttons in both card renderers"
      contains: "data-base"
    - path: "app/js/views/recipes.js"
      provides: "name+base dedup at modal + chip remove/lookup sites"
      contains: "Utils.sameRecipe"
  key_links:
    - from: "app/js/views/recommender.js toggle handlers"
      to: "Utils.sameRecipe"
      via: "list.some/filter comparison against item.recipe"
      pattern: "Utils\\.sameRecipe"
    - from: "app/js/views/recipes.js showRecipeDetail + chip handlers"
      to: "Utils.sameRecipe"
      via: "list.find/filter comparison against recipe"
      pattern: "Utils\\.sameRecipe"
---

<objective>
Implement D-08: duplicate guard by name + base spirit (case-insensitive) across the
Recommender and Recipes views. Add one shared `Utils.sameRecipe(a, b)` comparator and route
every name-only dedup site through it. Add `data-base` to the three action buttons in both
Recommender card renderers so toggle handlers can dedup by name+base end-to-end.

This plan is FOUNDATIONAL — D-06 (editable modal dual-write) and D-07 (Originals in
Recommender) both rely on name+base equality being correct.

Purpose: Two recipes with the same name but different bases must be distinct; toggling a
filled button removes the exact match; Favorites and Wishlist stay independent; made_log
increments times_made on re-add instead of duplicating.

Output: `Utils.sameRecipe` helper + updated dedup logic in recommender.js and recipes.js.

DONE work (do NOT re-plan — referenced as DONE with evidence):
- REC-10/REC-11 card layout + heart/star toggle state — recommender.js:84-88, 143-146
- RECIPE-MADE-01 ✓ button — recommender.js:87, 357-381
- RECIPE-MADE-02 Made tab — recipes.js:289-349
- RECIPE-VIEW-01/02 chips + clickable detail — recipes.js:231-287
- RECIPE-SEARCH-01 / REC-SEARCH-01 search inputs — recipes.js:49-63; recommender.js:487-493
- Originals-tab search wired (recipes.js:179), schema made_log/savedRecipe (schema lines 39-43, 134-174),
  modal live tally refresh (recipes.js:437-447) — the 3 prior gap tasks, confirmed DONE.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-recipe-recommender-ux/06-CONTEXT.md
@.planning/phases/06-recipe-recommender-ux/06-RESEARCH.md
@app/js/utils.js
@app/js/views/recommender.js
@app/js/views/recipes.js

<interfaces>
<!-- Existing Utils export (utils.js:111-112). Add sameRecipe to this returned object. -->
return { el, html, escapeHtml, today, formatDate, showToast, showLoading, showError,
         countInventoryItems, axisToValue, valueToAxisLabel };

<!-- Recommender card renderers build action buttons with data-name only (recommender.js:85-87, 143-145).
     Each carries class rec-fav-btn / rec-wish-btn / rec-made-btn. Add data-base to all six. -->
<button class="rec-fav-btn..." data-name="${Utils.escapeHtml(recipe.name)}" ...>
<button class="rec-wish-btn..." data-name="${Utils.escapeHtml(recipe.name)}" ...>
<button class="rec-made-btn..." data-name="${Utils.escapeHtml(recipe.name)}" ...>

<!-- Toggle handlers already look up the full item from allItems (recommender.js:316/341/366):
     const item = allItems.find(r => r.recipe.name === recipeName);
     item.recipe.base is in scope once the find is upgraded to name+base. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Utils.sameRecipe(a, b) comparator</name>
  <read_first>app/js/utils.js (escapeHtml at lines 26-32; export object at 111-112)</read_first>
  <files>app/js/utils.js</files>
  <behavior>
    - sameRecipe({name:'Negroni', base:'Gin'}, {name:'negroni', base:'GIN'}) === true (case-insensitive)
    - sameRecipe({name:'Negroni', base:'Gin'}, {name:'Negroni', base:'Mezcal'}) === false (base differs)
    - sameRecipe({name:'X'}, {name:'x'}) === true (both bases missing → '' === '')
    - sameRecipe(null, {name:'x'}) does not throw (guard with || {})
  </behavior>
  <action>
    Add a `sameRecipe(a, b)` function inside the Utils IIFE, before the `return` at line 111.
    Exact shape (D-08 unique key = name + base, case-insensitive):
    ```javascript
    function sameRecipe(a, b) {
      a = a || {}; b = b || {};
      return (a.name || '').toLowerCase() === (b.name || '').toLowerCase()
          && (a.base || '').toLowerCase() === (b.base || '').toLowerCase();
    }
    ```
    Add `sameRecipe` to the returned object (the `return { ... }` at line 111-112).
    Do NOT remove or rename any existing export.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/js/utils.js','utf8');if(!/function sameRecipe/.test(s))throw new Error('sameRecipe fn missing');if(!/return \{[\s\S]*sameRecipe[\s\S]*\}/.test(s))throw new Error('sameRecipe not exported');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' app/js/utils.js | grep -c 'function sameRecipe'` returns 1
    - the `return {` export line region contains `sameRecipe` (grep `sameRecipe` returns >= 2 matches: definition + export)
    - `node --check app/js/utils.js` exits 0 (no syntax error)
  </acceptance_criteria>
  <done>Utils.sameRecipe exists, is exported, and is case-insensitive on name AND base.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Route Recommender dedup through name+base (read checks, toggle handlers, data-base)</name>
  <read_first>app/js/views/recommender.js lines 59-165 (card renderers), 307-381 (toggle handlers)</read_first>
  <files>app/js/views/recommender.js</files>
  <action>
    Per D-08 + RESEARCH addendum call-site table. Replace name-only checks with Utils.sameRecipe.

    (a) Read/button-state checks — _renderCard (lines 63-65) and _renderTwoAwayCard (lines 121-123).
        Change each `.some(r => r.name === recipe.name)` to
        `.some(r => Utils.sameRecipe(r, recipe))` for confirmed_favorites, wishlist, made_log.

    (b) Add `data-base` to all three buttons in BOTH renderers (lines 85-87 and 143-145):
        add `data-base="${Utils.escapeHtml(recipe.base || '')}"` alongside the existing data-name.
        (escapeHtml prevents XSS via base text — see threat_model T-06-01.)

    (c) Toggle handlers (lines 308-381). In each of the three handlers:
        - Read base from the button: `const recipeBase = btn.dataset.base || '';`
          and build a probe `const probe = { name: recipeName, base: recipeBase };`
        - Upgrade the allItems lookup (lines 316/341/366) from
          `allItems.find(r => r.recipe.name === recipeName)` to
          `allItems.find(r => Utils.sameRecipe(r.recipe, probe))`.
        - Upgrade the isFav/isWish/isMade read (lines 317/342/367) to
          `.some(r => Utils.sameRecipe(r, probe))`.
        - Upgrade the remove filters (lines 319/344/369) from
          `.filter(f => f.name !== recipeName)` to
          `.filter(f => !Utils.sameRecipe(f, probe))` (same idea for w/m).
        - The add branches (lines 323-327/348-352/374-378) push `{ ...item.recipe, _source: 'classics-db', ... }`
          UNCHANGED in this plan. (D-07 changes the hardcoded _source later; do NOT touch it here.)
    Do not alter scoring, search, or scope logic.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const body=fs.readFileSync('app/js/views/recommender.js','utf8').split('\n').filter(l=>!/^\s*\/\//.test(l)).join('\n');const n=(body.match(/Utils\.sameRecipe/g)||[]).length;if(n<12)throw new Error('expected >=12 sameRecipe uses, got '+n);if(!/data-base=/.test(body))throw new Error('data-base missing');if(/r\.name === recipe\.name/.test(body))throw new Error('name-only check still present');console.log('OK '+n)" && node --check app/js/views/recommender.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' app/js/views/recommender.js | grep -c 'Utils.sameRecipe'` returns >= 12
    - `grep -c 'data-base=' app/js/views/recommender.js` returns >= 6 (3 buttons × 2 renderers)
    - `grep -c 'r.name === recipe.name' app/js/views/recommender.js` returns 0
    - `grep -c 'f.name !== recipeName' app/js/views/recommender.js` returns 0
    - `node --check app/js/views/recommender.js` exits 0
  </acceptance_criteria>
  <done>All six button-state reads, all three allItems lookups, all three isX reads, and all three remove filters use Utils.sameRecipe; all six buttons carry escaped data-base.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Route Recipes-view dedup through name+base (modal + chip handlers)</name>
  <read_first>app/js/views/recipes.js lines 276-283 (chip remove), 338-345 (made remove), 351-472 (showRecipeDetail)</read_first>
  <files>app/js/views/recipes.js</files>
  <action>
    Per D-08 + RESEARCH addendum recipes.js call-site table. Replace name-only checks with
    Utils.sameRecipe. Leave Originals id-based logic (recipes.js:492, 944) UNCHANGED.

    Sites to change (use `Utils.sameRecipe(x, recipe)` / `(m, recipe)` / `(r, recipe)`):
    - line 278: favorites/wishlist remove filter `x.name !== recipe.name` → `!Utils.sameRecipe(x, recipe)`
    - line 340: made remove filter `x.name !== recipe.name` → `!Utils.sameRecipe(x, recipe)`
    - line 353: `madeLog.find(m => m.name === recipe.name)` → `madeLog.find(m => Utils.sameRecipe(m, recipe))`
    - line 356: `[listKey].find(r => r.name === recipe.name)` → `Utils.sameRecipe(r, recipe)`
    - line 416: notes-save `[listKey].find(x => x.name === recipe.name)` → `Utils.sameRecipe(x, recipe)`
    - line 427: made-again `made_log.find(m => m.name === recipe.name)` → `Utils.sameRecipe(m, recipe)`
    - line 437: tally-refresh `made_log.find(m => m.name === recipe.name)` → `Utils.sameRecipe(m, recipe)`
    - line 449: reset filter `made_log.filter(m => m.name !== recipe.name)` → `!Utils.sameRecipe(m, recipe)`
    - line 464: unmade filter `made_log.filter(m => m.name !== recipe.name)` → `!Utils.sameRecipe(m, recipe)`
    Do NOT change the made-again add branch shape (line 432); it already uses `recipe._source || 'classics-db'`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const body=fs.readFileSync('app/js/views/recipes.js','utf8').split('\n').filter(l=>!/^\s*\/\//.test(l)).join('\n');const n=(body.match(/Utils\.sameRecipe/g)||[]).length;if(n<9)throw new Error('expected >=9 sameRecipe uses, got '+n);if(/m\.name === recipe\.name/.test(body))throw new Error('name-only made check remains');if(/x\.name !== recipe\.name/.test(body))throw new Error('name-only remove filter remains');console.log('OK '+n)" && node --check app/js/views/recipes.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' app/js/views/recipes.js | grep -c 'Utils.sameRecipe'` returns >= 9
    - `grep -c 'm.name === recipe.name' app/js/views/recipes.js` returns 0
    - `grep -c 'x.name !== recipe.name' app/js/views/recipes.js` returns 0
    - `grep -c 'x.id !== r.id' app/js/views/recipes.js` returns 1 (Originals id logic untouched, line 492)
    - `node --check app/js/views/recipes.js` exits 0
  </acceptance_criteria>
  <done>All nine modal/chip lookups use name+base via Utils.sameRecipe; Originals id-based delete/findIndex untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user recipe data → DOM (innerHTML) | recipe.name / recipe.base are user-controllable (saved from Recommender or authored Originals); rendered into card buttons and modal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering / Info-disclosure (XSS) | new `data-base` attribute in recommender.js card buttons | mitigate | Wrap base in `Utils.escapeHtml(recipe.base || '')` at every new data-base insertion (Task 2b). No raw recipe field reaches innerHTML. |
| T-06-02 | Tampering (XSS) | existing data-name attributes | accept | Already escaped (recommender.js:85-87); unchanged here. |
</threat_model>

<verification>
- Open Recommender; confirm no console error after the data-base/sameRecipe changes.
- Favorite a recipe, then favorite a same-name/different-base recipe → both appear in Favorites.
- Click a filled ♥ → that exact recipe leaves Favorites; click again → re-added; never duplicated.
- A recipe can be in Favorites AND Wishlist at once.
- From the detail modal, [+ Made It Again] on an already-made recipe increments ×N (no dup row).
</verification>

<success_criteria>
1. `Utils.sameRecipe` exists, exported, case-insensitive on name AND base.
2. recommender.js: >=12 sameRecipe uses; >=6 escaped data-base attrs; zero name-only checks in changed handlers.
3. recipes.js: >=9 sameRecipe uses; Originals id logic intact.
4. No new XSS sink (every data-base is escaped); all three files pass `node --check`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-recipe-recommender-ux/06-01-SUMMARY.md`
Suggested commit: `feat(06): dedup recipes by name+base via Utils.sameRecipe`
</output>
