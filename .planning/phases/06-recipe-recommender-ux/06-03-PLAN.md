---
phase: 06-recipe-recommender-ux
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - app/js/recommender-engine.js
  - app/js/views/recommender.js
  - app/css/app.css
autonomous: true
requirements:
  - REC-SEARCH-01
  - RECIPE-VIEW-02

must_haves:
  truths:
    - "User-authored Originals appear mixed into Recommender results alongside classics, each with an amber 'Your original' badge"
    - "Originals are matched against the user's actual inventory (Strategy B, inventory-aware) — not forced always-buildable"
    - "Rendering the Recommender with Originals present produces no console error (no keywords.map crash)"
    - "An Original lacking both a derivable base and ingredients is excluded from results"
    - "Favoriting/wishlisting/making an Original from the Recommender stores _source:'originals' so it opens editable per D-06"
  artifacts:
    - path: "app/js/recommender-engine.js"
      provides: "recommend accepts opts.originals; normalizeOriginal synthesizes keywords/searchIn; _hasIngredient guarded against missing keywords"
      contains: "normalizeOriginal"
    - path: "app/js/views/recommender.js"
      provides: "passes opts.originals at all recommend() call sites; 'Your original' badge; _source preserved on writes"
      contains: "Your original"
    - path: "app/css/app.css"
      provides: ".rec-original-badge amber styling"
      contains: "rec-original-badge"
  key_links:
    - from: "recommender.js recommend() call sites"
      to: "RecommenderEngine.recommend opts.originals"
      via: "State.get('recipes').originals passed in opts"
      pattern: "originals:"
    - from: "engine recommend loop"
      to: "normalizeOriginal + _hasIngredient guard"
      via: "Originals normalized into engine shape before matching"
      pattern: "normalizeOriginal"
---

<objective>
Implement D-07 with the LOCKED matching strategy — **Strategy B: inventory-aware matching**
(CONTEXT.md D-07, locked 2026-05-20). Originals join the Recommender scoring pool alongside
CLASSICS_DB and are matched against the user's actual inventory exactly like classics. This
requires normalizing each Original into the engine's expected shape, guarding the crash site,
preserving provenance on writes, and badging Originals in the UI.

NOTE ON STRATEGY: RESEARCH.md recommends Strategy A (always-buildable). CONTEXT.md D-07 LOCKS
Strategy B. Per user-decision fidelity, this plan implements Strategy B. The known tradeoff
(an Original whose ingredient names don't normalize to inventory tokens may not appear as
buildable) is accepted per D-07.

Purpose: Originals are discoverable in Recommendations with personalized, mood-based,
inventory-aware ranking.

Output: engine pool/normalization changes + view wiring + badge CSS.

DEPENDS ON 06-01 (name+base dedup so badged Originals toggle correctly) and 06-02 (editable
modal — the _source:'originals' write here is what makes a Recommender-favorited Original
editable). Touches recommender.js (after 06-01) and app.css (after 06-02) → wave 3.
</objective>

<execution_context>
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glestryc/personal/github_repos/barkeeper-bjorn-website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-recipe-recommender-ux/06-CONTEXT.md
@.planning/phases/06-recipe-recommender-ux/06-RESEARCH.md
@app/js/recommender-engine.js
@app/js/views/recommender.js

<interfaces>
<!-- Engine entry (recommender-engine.js:170). db hardcoded to CLASSICS_DB at line 172. -->
function recommend(inventory, rawProfile, opts = {}) { ... }
// opts today: { scope, ignoreVetoes, specialty }. Add: opts.originals (array).

<!-- CRASH SITE — _hasIngredient (recommender-engine.js:108-109): -->
const kws = ingredient.keywords.map(k => k.toLowerCase());  // throws if keywords undefined

<!-- Section keys (SECTION_MAP keys, engine:40-69) — used for searchIn synthesis (permissive = ALL keys): -->
// whiskey, brandy, rum, agave, white_spirits, other_spirits, fortified, liqueurs_fruit,
// liqueurs_nut, liqueurs_herbal, liqueurs_specialty, bitters_anchors, bitters_other,
// syrups, mixers, perishables, pantry, produce

<!-- DERIVATIONS map (engine:74-82) — already expands limes→lime juice etc. into the lookup. -->

<!-- recommend() call sites in recommender.js: lines 401, 413, 443, 481, 533 (all pass opts). -->

<!-- Original shape (data/recipes.json originals[*]): has id, name, ingredients[{amount,name,notes?}],
     method, glassware, garnish, profile(STRING). NO base, NO keywords, NO searchIn, NO tags. -->

<!-- Recommender hardcodes _source:'classics-db' on writes: recommender.js add branches
     (after 06-01 renumber) push { ...item.recipe, _source: 'classics-db', ... }. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine — accept opts.originals, normalize them, guard the crash site</name>
  <read_first>app/js/recommender-engine.js lines 40-129 (SECTION_MAP, DERIVATIONS, _hasIngredient), 164-227 (recommend)</read_first>
  <files>app/js/recommender-engine.js</files>
  <behavior>
    - recommend(inv, profile, { originals: [rawOriginal] }) includes the Original in results
      when its ingredients normalize to inventory matches (inventory-aware).
    - Feeding a raw Original (no keywords/searchIn) through the loop does NOT throw — _hasIngredient
      guards `ingredient.keywords` and `ingredient.searchIn` against undefined.
    - normalizeOriginal(o) returns an engine-shaped recipe: base derived from o.base ||
      o.ingredients[0].name; each ingredient gets keywords:[name.toLowerCase()] + tokenized words,
      searchIn = ALL SECTION_MAP keys (permissive), optional from /optional/i on notes; tags:[];
      _source:'originals'; carries name/method/glassware/garnish through.
    - Originals lacking a derivable base AND ingredients are excluded (filtered before scoring).
    - String profile yields neutral 0.5 (existing _flavorScore behavior, no crash).
  </behavior>
  <action>
    (a) Guard _hasIngredient (lines 108-111): replace `ingredient.keywords.map(...)` with
        `const kws = (ingredient.keywords || []).map(k => k.toLowerCase());` and iterate
        `for (const sectionKey of (ingredient.searchIn || []))`. (Defends against any unnormalized recipe.)

    (b) Add a module-local `ALL_SECTIONS = Object.keys(SECTION_MAP);` and a `normalizeOriginal(o)`:
        ```javascript
        function normalizeOriginal(o) {
          const ings = (o.ingredients || []).map(ing => {
            const nm = (ing.name || '').toLowerCase();
            const tokens = nm.split(/[^a-z0-9]+/).filter(t => t.length > 2);
            return {
              name: ing.name || '', amount: ing.amount || '',
              keywords: [nm, ...tokens].filter(Boolean),
              searchIn: ALL_SECTIONS,
              optional: /optional/i.test(ing.notes || ''),
            };
          });
          return {
            ...o,
            _source: 'originals',
            base: o.base || (o.ingredients && o.ingredients[0] && o.ingredients[0].name) || '',
            ingredients: ings,
            tags: o.tags || [],
          };
        }
        ```
    (c) In recommend(): build the pool. After `const db = ...` (line 172), add:
        ```javascript
        const rawOriginals = Array.isArray(opts.originals) ? opts.originals : [];
        const normOriginals = rawOriginals
          .filter(o => ((o.base || (o.ingredients && o.ingredients[0] && o.ingredients[0].name)) && (o.ingredients || []).length > 0))
          .map(normalizeOriginal);
        const pool = [...db, ...normOriginals];
        ```
        Change the main loop `for (const recipe of db)` (line 196) to `for (const recipe of pool)`.
        Leave the `if (!db.length) return ...` early-return as-is (originals still need db? — change
        to `if (!pool.length) return { buildable: [], oneAway: [], twoAway: [] };` so Originals show
        even if CLASSICS_DB were empty).
    (d) Export normalizeOriginal for testing: add to the returned object (line 230)
        `return { recommend, normalizeProfile: _normalizeProfile, normalizeOriginal };`
    Do not change scoring weights, veto logic, or sort order.
  </action>
  <verify>
    <automated>node -e "
      global.CLASSICS_DB = [];
      const fs=require('fs'); const vm=require('vm');
      const code=fs.readFileSync('app/js/recommender-engine.js','utf8');
      const ctx={CLASSICS_DB:[],console,String,Math,Object,Array,JSON}; vm.createContext(ctx);
      vm.runInContext(code,ctx);
      const inv={base_spirits:{whiskey:['Bourbon']},syrups:['Simple Syrup'],bitters:{anchors:['Angostura Bitters']}};
      const orig={id:'cocktail1',name:'Test Old Fashioned',ingredients:[{name:'Bourbon',amount:'2 oz'},{name:'Simple Syrup',amount:'0.25 oz'},{name:'Angostura Bitters',amount:'2 dashes'}],method:'stirred',glassware:'Rocks',profile:'rich and boozy'};
      const r=ctx.RecommenderEngine.recommend(inv,{},{originals:[orig]});
      const all=[...r.buildable,...r.oneAway,...r.twoAway];
      if(!all.some(x=>x.recipe.name==='Test Old Fashioned'))throw new Error('original not scored');
      if(!all.some(x=>x.recipe._source==='originals'))throw new Error('_source not preserved');
      // exclusion: no ingredients -> excluded
      const r2=ctx.RecommenderEngine.recommend(inv,{},{originals:[{id:'x',name:'Empty'}]});
      const all2=[...r2.buildable,...r2.oneAway,...r2.twoAway];
      if(all2.some(x=>x.recipe.name==='Empty'))throw new Error('empty original not excluded');
      // crash guard: raw original with no keywords must not throw (already exercised above)
      console.log('OK');
    " && node --check app/js/recommender-engine.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'normalizeOriginal' app/js/recommender-engine.js` returns >= 3 (def + use + export)
    - `grep -c '(ingredient.keywords || \[\])' app/js/recommender-engine.js` returns 1
    - `grep -c '(ingredient.searchIn || \[\])' app/js/recommender-engine.js` returns 1
    - `grep -c 'for (const recipe of pool)' app/js/recommender-engine.js` returns 1
    - the inline node test above prints `OK`; `node --check` exits 0
  </acceptance_criteria>
  <done>Engine accepts opts.originals, normalizes (keywords/searchIn/base/optional/tags/_source), guards the keywords/searchIn crash, excludes empty Originals, and matches inventory-aware (Strategy B).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: View — pass opts.originals at every call site; preserve _source on writes</name>
  <read_first>app/js/views/recommender.js call sites (lines ~401, 413, 443, 481, 533) and add-branch writes (favorites/wishlist/made handlers)</read_first>
  <files>app/js/views/recommender.js</files>
  <action>
    (a) At EACH of the five RecommenderEngine.recommend(...) calls, add
        `originals: State.get('recipes')?.originals || []` to the opts object literal.
        (Lines 401, 413, 443, 481, 533 — exact line numbers may shift after 06-01; match by the
        existing `{ scope: _scopeLevel, ignoreVetoes: _vetoOverrides, specialty: ... }` shape.)

    (b) Write-path _source (Pitfall D): in the three add branches of the toggle handlers, change
        the hardcoded `_source: 'classics-db'` to `_source: item.recipe._source || 'classics-db'`
        for confirmed_favorites, wishlist, AND made_log pushes. (After 06-01 these are the only
        remaining `'classics-db'` literals in the add branches.) This makes a Recommender-favorited
        Original store `_source:'originals'` so D-06 opens it editable.
    Do not change dedup logic (already name+base from 06-01).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/js/views/recommender.js','utf8');const calls=(s.match(/originals: State\.get\('recipes'\)\?\.originals/g)||[]).length;if(calls<5)throw new Error('expected 5 originals: opts, got '+calls);const preserved=(s.match(/_source: item\.recipe\._source \|\| 'classics-db'/g)||[]).length;if(preserved<3)throw new Error('expected 3 _source-preserving writes, got '+preserved);console.log('OK '+calls+'/'+preserved)" && node --check app/js/views/recommender.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "originals: State.get('recipes')?.originals" app/js/views/recommender.js` returns 5
    - `grep -c "_source: item.recipe._source || 'classics-db'" app/js/views/recommender.js` returns 3
    - `grep -c "_source: 'classics-db'" app/js/views/recommender.js` returns 0 (no remaining hardcode in add branches)
    - `node --check app/js/views/recommender.js` exits 0
  </acceptance_criteria>
  <done>All five recommend() calls pass opts.originals; all three add-branch writes preserve item.recipe._source.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: View + CSS — 'Your original' amber badge on Original cards</name>
  <read_first>app/js/views/recommender.js _renderCard (lines 59-113) and _renderTwoAwayCard (115-165); app/css/app.css .rec-times-made-badge (green badge template)</read_first>
  <files>app/js/views/recommender.js, app/css/app.css</files>
  <action>
    (a) In BOTH _renderCard and _renderTwoAwayCard, add a badge when
        `recipe._source === 'originals'`. Insert into `.rec-card-meta` after the difficulty span:
        `${recipe._source === 'originals' ? '<span class="rec-original-badge">Your original</span>' : ''}`
        The literal text is static (no user data) — no escaping needed; recipe._source is compared, not printed.
    (b) Append `.rec-original-badge` to app.css (after 06-02's modal CSS). Amber-colored, consistent
        with existing badge patterns (model on `.rec-times-made-badge` structure but amber): use
        `background`/`color`/`border` from `--amber`/`--amber-dim` custom properties, small font,
        inline-block, rounded (var(--radius-sm)).
    (c) (Cosmetic, accepted per Pitfall F) Difficulty chip shows "Advanced" for Originals
        (difficulty undefined). Leave as-is; note in UAT. Do not add difficulty to Originals.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const js=fs.readFileSync('app/js/views/recommender.js','utf8');const css=fs.readFileSync('app/css/app.css','utf8');const b=(js.match(/rec-original-badge/g)||[]).length;if(b<2)throw new Error('expected badge in both renderers, got '+b);if(!/Your original/.test(js))throw new Error('badge text missing');if(!/rec-original-badge/.test(css))throw new Error('badge CSS missing');console.log('OK')" && node --check app/js/views/recommender.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'rec-original-badge' app/js/views/recommender.js` returns >= 2 (both renderers)
    - `grep -c 'Your original' app/js/views/recommender.js` returns >= 2
    - `grep -c 'rec-original-badge' app/css/app.css` returns >= 1
    - CSS block references `--amber` (grep `var(--amber` in the new block returns >= 1)
    - `node --check app/js/views/recommender.js` exits 0
  </acceptance_criteria>
  <done>Originals carry an amber 'Your original' badge in both card layouts; badge styled via amber custom properties.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored Original fields → engine + DOM | Original name/ingredients flow into normalizeOriginal (engine, no DOM) and into card rendering (innerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Denial of Service (crash) | _hasIngredient on unnormalized Original | mitigate | Guard `ingredient.keywords`/`searchIn` with `|| []` (Task 1a); normalizeOriginal always supplies both. Verified by inline engine test. |
| T-06-07 | Tampering (XSS) | Original name/ingredients in card innerHTML | mitigate | Existing card renderers already wrap recipe.name/base/ingredient fields in Utils.escapeHtml (recommender.js:70-95); Originals flow through the same escaped path. Badge text is a static literal. |
| T-06-08 | Tampering (XSS) | badge | accept | Badge text "Your original" is a constant; no interpolation. |
</threat_model>

<verification>
- Author an Original whose ingredients exist in inventory; open Recommender → it appears in
  "You Can Make These" with an amber 'Your original' badge, no console error.
- Author an Original with no ingredients → it does NOT appear.
- Favorite an Original from the Recommender → inspect data/recipes.json: the confirmed_favorites
  entry has `_source:'originals'`; open it via the Favorites chip → fields are editable (D-06).
- Adjust mood sliders → Originals re-rank (string profile → neutral 0.5; ranking driven by classics).
</verification>

<success_criteria>
1. Originals are scored inventory-aware (Strategy B) and mixed into results with an amber badge.
2. No keywords.map / searchIn crash (engine guard + normalization); inline engine test prints OK.
3. Originals lacking base AND ingredients are excluded.
4. Recommender writes preserve _source so Originals open editable per D-06.
5. recommender-engine.js + recommender.js pass `node --check`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-recipe-recommender-ux/06-03-SUMMARY.md`
Suggested commit: `feat(06): score Originals in Recommender (inventory-aware) with badge`
</output>
