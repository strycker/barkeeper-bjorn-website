---
phase: 05-polish-depth-ux-tidy
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/05-polish-depth-ux-tidy/TEST-CHECKLIST.md
autonomous: true
requirements: [REC-05, REC-06, REC-07, REC-08, REC-09, INV-08, INV-09, INV-10, DATA-01, DATA-02, DATA-03, CUST-01, CUST-02]

must_haves:
  truths:
    - "A test checklist exists covering every Phase 5 REQ ID"
    - "Each REQ ID maps to at least one concrete, browser-verifiable test step"
    - "Tests reference exact URLs (http://localhost:8000/app/#<route>), exact strings to type, and exact JSON shapes to verify post-save"
  artifacts:
    - path: ".planning/phases/05-polish-depth-ux-tidy/TEST-CHECKLIST.md"
      provides: "Manual UAT checklist for Phase 5"
      contains: "REC-05, REC-06, REC-07, REC-08, REC-09, INV-08, INV-09, INV-10, DATA-01, DATA-02, DATA-03, CUST-01, CUST-02"
  key_links:
    - from: "TEST-CHECKLIST.md REC-09 test"
      to: "app/js/recommender-engine.js _expandLookup behavior"
      via: "Test states: with limes only in inventory, recipe needing 'lime juice' appears in buildable section"
      pattern: "REC-09.*lime"
---

<objective>
Create the Phase 5 manual UAT test checklist BEFORE any implementation work begins.
Every Phase 5 requirement (13 IDs) must have at least one concrete, browser-verifiable test step. Implementer plans (05-01..05-04) will be measured against this checklist.

Purpose: Establish the verification surface up-front so executors cannot ship code that "looks done" but fails user-visible behavior.
Output: `.planning/phases/05-polish-depth-ux-tidy/TEST-CHECKLIST.md`
</objective>

<execution_context>
@/home/user/barkeeper-bjorn-website/CLAUDE.md
@/home/user/barkeeper-bjorn-website/.planning/phases/05-polish-depth-ux-tidy/05-CONTEXT.md
@/home/user/barkeeper-bjorn-website/.planning/phases/05-polish-depth-ux-tidy/05-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-polish-depth-ux-tidy/05-CONTEXT.md
@.planning/phases/05-polish-depth-ux-tidy/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TEST-CHECKLIST.md for Phase 5</name>
  <files>.planning/phases/05-polish-depth-ux-tidy/TEST-CHECKLIST.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Phase 5 section, REC-05–CUST-02)
    - .planning/phases/05-polish-depth-ux-tidy/05-CONTEXT.md (D-01..D-33 for behavior specifics)
    - .planning/phases/05-polish-depth-ux-tidy/05-RESEARCH.md (§"Phase Requirements → Test Map (manual)" — table at lines 296–310 is the seed)
    - .planning/phases/02-web-ui-ux-and-settings/TEST-CHECKLIST.md (existing analog format) IF it exists; otherwise mirror the table style from RESEARCH.md
  </read_first>
  <action>
Create `.planning/phases/05-polish-depth-ux-tidy/TEST-CHECKLIST.md` as a Markdown document with the following structure:

```markdown
# Phase 5: Polish, Depth & UX Tidy — Test Checklist

**Setup:** `python3 -m http.server 8000` from repo root. Open http://localhost:8000/app/ in a fresh browser session (or after clearing localStorage `bb_token` is still set). All test steps assume a configured GitHub PAT in Settings.

---

## REC — Recommender UX

### REC-05 — Cumulative scope highlight
- [ ] Navigate to http://localhost:8000/app/#recommender
- [ ] Click scope button labeled "Allow 2 missing" — verify buttons "Only what I have", "Allow 1 missing", AND "Allow 2 missing" all carry the `.active` CSS class (inspect via DevTools or visible highlight)
- [ ] Click "Unconstrained" — verify all four scope buttons show `.active`
- [ ] Click "Only what I have" — verify ONLY that button shows `.active`

### REC-06 — Unconstrained 4th button
- [ ] On http://localhost:8000/app/#recommender, confirm a 4th scope button labeled exactly "Unconstrained" exists with `data-scope="3"`
- [ ] Click "Unconstrained" — verify recipe cards appear that the user cannot fully build (e.g. recipes requiring spirits not in inventory)
- [ ] With a veto configured (Inventory → Vetoes contains an ingredient that appears in some recipe), click "Unconstrained" — verify recipes whose base spirit matches the veto are still excluded by default

### REC-07 — Vetoes filter panel
- [ ] On #recommender, with at least one entry in `inventory.json → vetoes.disliked_ingredients`, verify a "Vetoes" panel appears in the sidebar below the mood sliders and chips
- [ ] If `vetoes.disliked_ingredients` is empty, verify the panel displays the text "No vetoes configured — add them in Inventory → Vetoes." with NO section header
- [ ] Click a veto chip to toggle it OFF — chip displays with `text-decoration: line-through` and `opacity: 0.5`; recipe results update to include the previously vetoed ingredient
- [ ] Navigate to #dashboard then back to #recommender — verify the veto chip is back to its enforced (non-strikethrough) state (silent reset; no toast)

### REC-08 — Favorites / Wishlist quick-actions
- [ ] On #recommender, every recipe card shows two icon buttons in the top-right area: ♥ (favorite) and ☆ (wishlist)
- [ ] Click ♥ on a card for a recipe NOT already in favorites — toast "Added to Favorites" appears; after refresh, `data/recipes.json → confirmed_favorites[]` contains an entry with that recipe's `name`
- [ ] Click ♥ on the same card again — toast "Already in Favorites" appears (info style); no duplicate added to `confirmed_favorites[]`
- [ ] Click ☆ on a different card — toast "Added to Wishlist"; `data/recipes.json → wishlist[]` contains the entry

### REC-09 — Ingredient derivation map
- [ ] Add "limes" to inventory (any section like produce/perishables); ensure "lime juice" is NOT present
- [ ] On #recommender, verify a recipe whose ingredients include "lime juice" (e.g. Daiquiri, Margarita, Gimlet from classics-db.js) appears in the "Only what I have" buildable list — proving _expandLookup added "lime juice" because "lime" is present
- [ ] Repeat with: sugar → simple syrup; eggs → egg white; mint → muddled mint; cream → heavy cream; honey → honey syrup; lemons → lemon juice (pick one classics-db recipe per pair)
- [ ] grep -nE "function _expandLookup|const DERIVATIONS" app/js/recommender-engine.js → both must match

---

## INV — Inventory Depth

### INV-08 — Field label renames
- [ ] On http://localhost:8000/app/#inventory, click any bottle chip to open the inline edit form
- [ ] Verify the form shows `<label>` text "Category" (not "Style") for the `data-field="style"` input
- [ ] Verify the `data-field="style"` input's placeholder is exactly `e.g. Bourbon, Gin, Mezcal`
- [ ] Verify the form shows `<label>` text "Specific Style/Type" (not "Type") for `data-field="type"`
- [ ] Verify the `data-field="type"` input's placeholder is exactly `e.g. Single Barrel, Cask Strength, Espadin`
- [ ] Verify underlying JSON keys are unchanged: edit + save → `inventory.json` still contains `style` and `type` (not `category`)

### INV-09 — Nationality field
- [ ] Open bottle inline edit form; expand "More fields" section
- [ ] Verify an input with `data-field="nationality"` exists with placeholder `e.g. Scotland, Mexico, Kentucky USA`
- [ ] Type "Scotland" → click Save → `data/inventory.json` shows the updated bottle has `"nationality": "Scotland"`
- [ ] grep -n '"nationality"' schema/inventory.schema.json → must match (schema updated)
- [ ] grep -n 'nationality' app/js/normalize.js → must match (default coercion added)

### INV-10 — Quick-add bar (regression-only; shipped at commit 6955f51)
- [ ] On http://localhost:8000/app/#inventory, verify a quick-add input bar exists at the top of the page (above tabs)
- [ ] Type "Bulleit Bourbon" + Enter → toast confirmation; bottle lands in Spirits tab under whiskey/bourbon section (per QUICK_ADD_RULES)
- [ ] Type "Plymouth Gin" + Enter → lands in gin section
- [ ] Type "Del Maguey Vida Mezcal" + Enter → lands in mezcal section
- [ ] Type "Mystery liquid xyz" + Enter → inline `<select>` picker appears showing BOTTLE_SECTIONS; selecting one + Confirm adds the bottle to that section
- [ ] grep -n "QUICK_ADD_RULES\|renderQuickAddBar" app/js/views/inventory.js → both must match (no re-implementation)

---

## DATA — Data Model Tidy

### DATA-01 — Equipment consolidation
- [ ] In a browser session: with `data/bar-owner-profile.json` containing an `equipment` field (e.g. `{shaker:"cobbler",jigger:true}`), reload the app
- [ ] Trigger any profile save (e.g. Profile tab → adjust slider → Save) → after save, fetch `data/bar-owner-profile.json` from GitHub and verify the top-level `equipment` field is GONE
- [ ] Same test for `data/barkeeper.json` if it contains an equipment field — must be stripped on save
- [ ] Re-run onboarding: at the equipment step, check boxes for shaker / jigger / strainer + select Hawthorne → complete onboarding → `data/inventory.json → equipment` contains ALL non-empty selections; `data/bar-owner-profile.json` has NO `equipment` key
- [ ] grep -n 'delete out.equipment' app/js/normalize.js → must match in barkeeper() AND profile() blocks
- [ ] grep -n '"shaker"\|"mixing_glass"\|"jigger"\|"bar_spoon"\|"strainer"\|"citrus_press"\|"ice_setup"' schema/inventory.schema.json → all 7 must appear under `equipment.properties` (per D-33)

### DATA-02 — Numeric axis migration
- [ ] With `data/bar-owner-profile.json` having any axis position as a string (e.g. `"position": "Lean A"`), reload app → trigger profile save → verify after save, `position` is a float (0.25 for "Lean A")
- [ ] On http://localhost:8000/app/#profile, drag a flavor axis slider to a new value and click Save → reload → verify the saved `position` is the raw float the slider produced (e.g. 0.6, 0.85), NOT a string label
- [ ] grep -nE 'position\s*=\s*newVal' app/js/views/profile.js → must match (line ~253 fix verified)
- [ ] grep -n 'valueToAxisLabel' app/js/views/profile.js → must NOT match in the slider input handler (the legacy write is gone)
- [ ] On #profile, verify the flavor section shows NO "Lean A / Strong A / Middle / Lean B / Strong B" heading labels; only sliders + pole labels (e.g. "Sweet ← → Dry")

### DATA-03 — Rich profile fields
- [ ] Reset/start onboarding fresh → progress to the new Step 7 titled "About Your Drinking Style — optional"
- [ ] Verify Step 7 has a visible "Skip" button
- [ ] Verify Step 7 includes: drinking_frequency `<select>` (options: daily / several times a week / weekly / occasionally / rarely); household_context text input with placeholder `e.g. Couple, hosting often`; vocabulary_preference `<select>` (casual / balanced / technical); archetype chip grid with 6 chips: The Minimalist, The Experimenter, The Host, The Purist, The Adventurer, The Classicist
- [ ] Complete Step 7 selecting 2 archetypes → `data/bar-owner-profile.json → background.drinking_frequency` etc. populated; `archetypes[]` contains 2 objects each with `name` and `description`
- [ ] On http://localhost:8000/app/#profile, scroll to bottom → verify a collapsible "Drinking Style" section is present, collapsed by default (▾ icon)
- [ ] Expand the section → all 4 fields are inline-editable; editing triggers the existing sticky save bar; Save persists to GitHub

---

## CUST — Bartender Customization

### CUST-01 — Bartender Customization Wizard
- [ ] Navigate to http://localhost:8000/app/#bartender-wizard → page renders without errors (single-page scrollable form)
- [ ] Form contains the following inputs/textareas: name; avatar_url (URL text input); "Upload from file" button; voice preset dropdown; personality_description textarea (~5 rows); behavioral_rules add/remove list with "+ Add Rule" button; cocktail_naming_style; image_gen_style textarea; signoff single-line input
- [ ] Edit name → sticky save bar appears at bottom — id `bw-save-bar` with text "Unsaved changes" and Save + Discard buttons
- [ ] Click Save → toast "Bartender saved to GitHub ✓"; `data/barkeeper.json` reflects the new name and all wizard fields
- [ ] Add 2 behavioral rules + click × on one → list correctly updates; Save persists `behavioral_rules` as a string[]
- [ ] Click "Upload from file" + select a small image → file uploads to `images/barkeeper_avatar_<timestamp>.<ext>`; `barkeeper.avatar_url` is populated with the raw GitHub URL; URL field reflects the new value (single source of truth)
- [ ] grep -n "GitHubAPI.writeFile\|GitHubAPI.getFileSHA" app/js/views/bartender-wizard.js → both must match (correct API, NOT a non-existent `uploadImage`)
- [ ] grep -n "case 'bartender-wizard'" app/js/app.js → must match
- [ ] grep -n "bartender-wizard.js" app/index.html → must match

### CUST-02 — Settings link
- [ ] Navigate to http://localhost:8000/app/#settings → Bartender Identity section still shows Name input + Preset dropdown
- [ ] Verify a button/link "Full Customization →" appears in that section
- [ ] Click it → URL hash becomes `#bartender-wizard`; wizard view renders
- [ ] grep -n "Full Customization\|#bartender-wizard" app/js/views/settings.js → must match

---

## Success criteria summary

All 13 boxes above (one per REQ ID) must be checked before Phase 5 is considered complete. Plans 05-01..05-04 reference this file in their `verify` and `done` blocks.
```

This file is the spec for downstream executors. Do not add features beyond what each REQ ID specifies. Do not edit any code in this task — markdown only.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-polish-depth-ux-tidy/TEST-CHECKLIST.md && grep -cE '^### (REC-0[5-9]|INV-(08|09|10)|DATA-0[1-3]|CUST-0[1-2])' .planning/phases/05-polish-depth-ux-tidy/TEST-CHECKLIST.md | grep -qx 13</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-polish-depth-ux-tidy/TEST-CHECKLIST.md` exists
    - `grep -c '^### REC-' .../TEST-CHECKLIST.md` returns 5 (REC-05..REC-09)
    - `grep -c '^### INV-' .../TEST-CHECKLIST.md` returns 3 (INV-08..INV-10)
    - `grep -c '^### DATA-' .../TEST-CHECKLIST.md` returns 3 (DATA-01..DATA-03)
    - `grep -c '^### CUST-' .../TEST-CHECKLIST.md` returns 2 (CUST-01..CUST-02)
    - `grep -c 'http://localhost:8000/app/#' .../TEST-CHECKLIST.md` returns ≥ 6 (concrete URLs)
    - `grep -c 'grep -' .../TEST-CHECKLIST.md` returns ≥ 8 (grep-verifiable conditions sprinkled across REQs)
    - Document contains the regression-only language for INV-10 ("shipped at commit 6955f51")
    - Document contains DATA-01 schema check for all 7 equipment field names
  </acceptance_criteria>
  <done>TEST-CHECKLIST.md committed; 13 REQ IDs covered; quality gate above passes.</done>
</task>

</tasks>

<verification>
- All 13 Phase 5 REQ IDs (REC-05/06/07/08/09, INV-08/09/10, DATA-01/02/03, CUST-01/02) have a `### REQ-ID` heading in TEST-CHECKLIST.md.
- Every test step is either a browser action with an exact URL, a grep, or a JSON-shape assertion. No subjective language.
</verification>

<success_criteria>
TEST-CHECKLIST.md exists, covers all 13 IDs, and is referenced by plans 05-01..05-04 for final verification.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-depth-ux-tidy/05-00-SUMMARY.md` summarizing the checklist creation (REQ coverage, file size, any deviations from RESEARCH.md test seed table).
</output>
