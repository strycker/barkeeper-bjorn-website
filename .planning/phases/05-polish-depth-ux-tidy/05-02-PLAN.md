---
phase: 05-polish-depth-ux-tidy
plan: 02
type: execute
wave: 1
depends_on: [05-00]
files_modified:
  - app/js/views/inventory.js
  - app/js/normalize.js
  - schema/inventory.schema.json
autonomous: false
requirements: [INV-08, INV-09, INV-10]

must_haves:
  truths:
    - "Bottle edit form shows 'Category' (not 'Style') and 'Specific Style/Type' (not 'Type') with example placeholders"
    - "Underlying JSON keys for those fields remain 'style' and 'type' (no data migration)"
    - "Bottle edit form 'More fields' expanded section has a Nationality free-text input"
    - "Saving a bottle with Nationality persists nationality to inventory.json bottle entry"
    - "schema/inventory.schema.json bottleEntry includes a 'nationality' string property"
    - "normalize.js coerces missing bottle.nationality to empty string"
    - "Quick-add bar (shipped at commit 6955f51) still works: typing 'Bulleit Bourbon' + Enter lands in base_spirits.whiskey; typing 'Mystery liquid' + Enter opens inline section picker"
    - "Nationality is NOT shown on the bottle chip face (only inside edit form)"
  artifacts:
    - path: "app/js/views/inventory.js"
      provides: "Renamed labels (Category / Specific Style/Type), Nationality field in expanded fields"
      contains: "Specific Style/Type"
    - path: "app/js/normalize.js"
      provides: "Default nationality:'' coercion on every bottle entry"
      contains: "nationality"
    - path: "schema/inventory.schema.json"
      provides: "nationality string property in bottleEntry"
      contains: "\"nationality\""
  key_links:
    - from: "Bottle edit form Nationality input"
      to: "inventory.json bottle entry"
      via: "existing formEl.querySelectorAll('[data-field]') save loop at inventory.js:349-352"
      pattern: "data-field=\"nationality\""
    - from: "Quick-add bar"
      to: "QUICK_ADD_RULES array (inventory.js:88-108)"
      via: "renderQuickAddBar (inventory.js:428-514)"
      pattern: "QUICK_ADD_RULES"
---

<objective>
Inventory entry depth: rename Style→Category and Type→Specific Style/Type labels with placeholders (INV-08); add an optional Nationality free-text field to the bottle edit form + schema + normalize (INV-09); regression-verify the already-shipped quick-add bar (INV-10).

Purpose: Make bottle entry clearer (Category vs Specific is a recurring confusion); allow capturing origin (asked-for in Phase 5 scope); confirm the INV-10 fast path still parses canonical inputs after Phase 5 edits to neighboring code.
Output: Edits to inventory.js + normalize.js + inventory.schema.json. No new files. No re-implementation of INV-10.
</objective>

<execution_context>
@/home/user/barkeeper-bjorn-website/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/barkeeper-bjorn-website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-polish-depth-ux-tidy/05-CONTEXT.md
@.planning/phases/05-polish-depth-ux-tidy/05-RESEARCH.md
@.planning/phases/05-polish-depth-ux-tidy/05-PATTERNS.md
@.planning/phases/05-polish-depth-ux-tidy/TEST-CHECKLIST.md
@app/js/views/inventory.js
@app/js/normalize.js
@schema/inventory.schema.json

<interfaces>
Existing bottle edit form save loop (inventory.js:349-352) auto-picks up any `[data-field]` input — adding a new `data-field="nationality"` input requires NO save handler change.

Existing INV-10 quick-add bar (inventory.js:88-108 QUICK_ADD_RULES, lines 428-514 renderQuickAddBar) — DO NOT MODIFY. This task verifies regression only.

Schema location: schema/inventory.schema.json, `bottleEntry` definition (~lines 151-172).
Normalize location: normalize.js bottle coercion block (within `inventory()` normalizer).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Label renames + Nationality field + schema + normalize (INV-08, INV-09)</name>
  <files>app/js/views/inventory.js, app/js/normalize.js, schema/inventory.schema.json</files>
  <read_first>
    - app/js/views/inventory.js (focus lines 88–108 QUICK_ADD_RULES; 300–330 openEditForm label region; 340–360 save loop; 428–514 renderQuickAddBar — DO NOT MODIFY)
    - app/js/normalize.js (full file; the bottle-array coercion block in the `inventory()` normalizer)
    - schema/inventory.schema.json (lines 130–179, bottleEntry definition)
    - .planning/phases/05-polish-depth-ux-tidy/05-PATTERNS.md §"app/js/views/inventory.js (MOD)"
    - .planning/phases/05-polish-depth-ux-tidy/05-CONTEXT.md D-15, D-16, D-17, D-18
  </read_first>
  <action>
**A. INV-08 label-only renames in `openEditForm` (inventory.js ~lines 306-307):**

Change the bottle edit form so the JSON keys stay `style` and `type`, but the labels and placeholders match D-15 verbatim:
```html
<label>Category
  <input type="text" data-field="style" placeholder="e.g. Bourbon, Gin, Mezcal"
         value="${Utils.escapeHtml(entry.style || '')}">
</label>
<label>Specific Style/Type
  <input type="text" data-field="type" placeholder="e.g. Single Barrel, Cask Strength, Espadin"
         value="${Utils.escapeHtml(entry.type || '')}">
</label>
```
Tooltip via `title` attribute (D-15 says "tooltip" — add `title="Broad category (Bourbon, Gin, Mezcal, etc.)"` on the Category input and `title="The specific bottle's style/type (e.g. Single Barrel, Espadin)"` on the Type input).

DO NOT change the `data-field` attribute values (must stay `style` and `type` per D-15).

**B. INV-09 Nationality field — add to "More fields" expanded block (inventory.js ~lines 311-325):**

Add alongside Brand and Tier:
```html
<label>Nationality
  <input type="text" data-field="nationality"
         placeholder="e.g. Scotland, Mexico, Kentucky USA"
         value="${Utils.escapeHtml(entry.nationality || '')}">
</label>
```
The existing save loop `formEl.querySelectorAll('[data-field]')` at ~line 349-352 auto-picks this up — DO NOT add a separate handler.

Per D-18, do NOT render nationality on the bottle chip face. Verify the chip-rendering region (look for the bottle chip template — `bottle-chip` class) and confirm no change is needed.

**C. Schema update (schema/inventory.schema.json, bottleEntry ~lines 151-172):**

Add after the existing `notes` property:
```json
"nationality": {
  "type": "string",
  "default": "",
  "description": "Country/region of origin (e.g. Scotland, Mexico, Kentucky USA)."
}
```
Maintain valid JSON (trailing commas, etc.).

**D. Normalize.js bottle coercion:**

In the bottle-entry coercion block inside `inventory()` (look for where existing fields like `style`, `type`, `brand`, `tier` are normalized), add:
```js
nationality: ensureString(entry.nationality),
```
Pattern follows the existing `ensureString` usage on string fields. Idempotent — if nationality already exists it's preserved; if missing it defaults to `""`.
  </action>
  <verify>
    <automated>grep -c "Specific Style/Type" /home/user/barkeeper-bjorn-website/app/js/views/inventory.js | grep -q "^1$" && grep -c ">Category<" /home/user/barkeeper-bjorn-website/app/js/views/inventory.js | grep -q "^[1-9]" && grep -c "data-field=\"nationality\"" /home/user/barkeeper-bjorn-website/app/js/views/inventory.js | grep -q "^1$" && grep -c "data-field=\"style\"" /home/user/barkeeper-bjorn-website/app/js/views/inventory.js | grep -q "^[1-9]" && grep -c "data-field=\"type\"" /home/user/barkeeper-bjorn-website/app/js/views/inventory.js | grep -q "^[1-9]" && grep -c "\"nationality\"" /home/user/barkeeper-bjorn-website/schema/inventory.schema.json | grep -q "^[1-9]" && grep -c "nationality" /home/user/barkeeper-bjorn-website/app/js/normalize.js | grep -q "^[1-9]" && python3 -c "import json; json.load(open('/home/user/barkeeper-bjorn-website/schema/inventory.schema.json'))" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n ">Category<" app/js/views/inventory.js` returns at least one match (the renamed label)
    - `grep -n "Specific Style/Type" app/js/views/inventory.js` returns one match
    - `grep -n "data-field=\"style\"" app/js/views/inventory.js` still finds the Category input (key unchanged)
    - `grep -n "data-field=\"type\"" app/js/views/inventory.js` still finds the Specific Style/Type input
    - `grep -n "e.g. Bourbon, Gin, Mezcal" app/js/views/inventory.js` returns one match (placeholder)
    - `grep -n "e.g. Single Barrel, Cask Strength, Espadin" app/js/views/inventory.js` returns one match
    - `grep -n "data-field=\"nationality\"" app/js/views/inventory.js` returns exactly one match
    - `grep -n "e.g. Scotland, Mexico, Kentucky USA" app/js/views/inventory.js` returns one match
    - `python3 -c "import json; print(json.load(open('schema/inventory.schema.json'))['properties']['bottles']['items']['additionalProperties'] if False else 'ok')"` — placeholder; instead: `python3 -c "import json; d=json.load(open('schema/inventory.schema.json')); assert 'nationality' in str(d), 'nationality missing'; print('OK')"` succeeds
    - `grep -n "nationality" app/js/normalize.js` returns at least one match in a bottle-coercion context
    - schema JSON parses without error (Python json.load succeeds)
    - QUICK_ADD_RULES region (inventory.js:88-108) and renderQuickAddBar (lines 428-514) are UNCHANGED — confirm with `git diff app/js/views/inventory.js` after editing: only lines in the openEditForm region should be in the diff
  </acceptance_criteria>
  <done>Label renames live, Nationality field renders + persists, schema + normalize updated. Quick-add bar code untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — INV-08, INV-09, INV-10 regression</name>
  <what-built>
Label renames (Category, Specific Style/Type) and Nationality field in the bottle edit form; schema + normalize updates for nationality. INV-10 quick-add bar untouched but regression-verified.
  </what-built>
  <how-to-verify>
1. Start server: `python3 -m http.server 8000` and open http://localhost:8000/app/#inventory.

2. **INV-08:** Click the edit (pencil) icon on any bottle. The form should show "Category" (not "Style") with placeholder "e.g. Bourbon, Gin, Mezcal", and "Specific Style/Type" (not "Type") with placeholder "e.g. Single Barrel, Cask Strength, Espadin". Hover each input — tooltip should appear.

3. **INV-09:** In the edit form, expand "More fields". A "Nationality" text input should appear next to Brand/Tier with placeholder "e.g. Scotland, Mexico, Kentucky USA". Type "Kentucky USA", click Save. Open https://github.com/{owner}/{repo}/blob/{branch}/data/inventory.json — find the bottle entry; it should now have `"nationality": "Kentucky USA"`. The bottle's chip face on the Inventory page should NOT display the nationality (only style + brand as before).

4. **INV-09 (defaults):** Find any existing bottle that has no nationality. Reload the app. The Network/Console tab should show no errors — `nationality` should default to `""` after normalize. Open the edit form; the Nationality field renders empty.

5. **INV-10 regression — canonical parsing:** At the top of the Inventory page (above the tabs), use the quick-add bar. Type `Bulleit Bourbon` and press Enter. A toast should confirm the add; switching to the Spirits tab should show "Bulleit Bourbon" under the Whiskey section. Repeat with these canonical inputs (all should match a QUICK_ADD_RULES rule and land in the right section):
   - `Hendrick's Gin` → Gin
   - `Don Julio Tequila` → Tequila
   - `Montelobos Mezcal` → Mezcal
   - `Bacardi Rum` → Rum

6. **INV-10 regression — unknown handling:** Type `Mystery liquid 47` + Enter. Instead of an instant add, an inline section-picker (`<select>` of all BOTTLE_SECTIONS) should appear with a Confirm button. Pick a section, confirm — the item should be added there.

7. Confirm no console errors throughout.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (cite INV-XX number)</resume-signal>
</task>

</tasks>

<verification>
TEST-CHECKLIST.md items INV-08, INV-09, INV-10 all pass. The INV-10 quick-add bar diff is zero (`git diff app/js/views/inventory.js` shows only openEditForm-region edits).
</verification>

<success_criteria>
1. Edit form shows "Category" + "Specific Style/Type" labels with placeholders (INV-08).
2. Nationality input present in expanded fields; persists to inventory.json; not on chip face (INV-09).
3. QUICK_ADD_RULES + renderQuickAddBar code untouched; regression checklist passes (INV-10).
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-depth-ux-tidy/05-02-SUMMARY.md` noting:
- Confirmation of zero edits to QUICK_ADD_RULES / renderQuickAddBar regions
- Any tooltip phrasing variations used
- Schema diff (added property)
</output>
